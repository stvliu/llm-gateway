# pnpm 11 构建脚本白名单。
# pnpm 11 起不再读取 package.json 的 pnpm 字段，改用 pnpm-workspace.yaml 的 allowBuilds。
# 未配置时这些包的构建脚本被忽略（ERR_PNPM_IGNORED_BUILDS，exit code 1），
# 会导致 astro dev 前置依赖检查中断。下列依赖需要构建脚本以生成平台二进制。
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
