---
title: SDK 使用
description: 多语言 SDK 与原生 SDK 接入
---

# SDK 使用

> ⚠️ **官方多语言 SDK（Python / TypeScript / Go）为规划功能（v2.0），当前版本尚未提供。** 当前请使用原生 OpenAI / Anthropic SDK 指向网关。

## 当前方式：原生 SDK

网关兼容 OpenAI 与 Anthropic 标准，现有 SDK 只需修改 `base_url` 与 `api_key` 即可接入。

### Python（OpenAI SDK）

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxxxxxxx",          # 网关 API Key
    base_url="http://localhost:8080/v1"
)
```

### Node.js（OpenAI SDK）

```javascript
const client = new OpenAI({
  apiKey: "sk-xxxxxxxx",
  baseURL: "http://localhost:8080/v1",
});
```

### Python（Anthropic SDK）

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="sk-xxxxxxxx",
    base_url="http://localhost:8080"
)
```

> 更多示例（流式、多轮、协议互转）见 [API 调用示例](/api/examples/)。

## 规划中的官方 SDK

v2.0 计划提供 Python / TypeScript / Go 官方 SDK，兼容 OpenAI SDK 接口，自动处理认证、重试、错误处理。
