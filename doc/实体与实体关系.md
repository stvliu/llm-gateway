# 实体与实体关系文档

> **文档版本**: v5.0
> **依据文档**: specs/001-provider-adapter/data-model.md、LLM-Gateway 架构设计
> **生成日期**: 2026-04-23
> **状态**: 已更新至新架构

---

## 一、文档概述

本文档基于 LLM-Gateway 项目需求规格说明书，定义企业级 AI 模型 API 聚合分发与智能路由网关的实体模型与关系网络。

### 1.1 系统定位差异

| 维度 | LLM-Gateway | APIPark |
|------|-------------|---------|
| **目标用户** | 企业中台/大型团队/政企客户 | 企业 API 开放平台/多租户 SaaS |
| **核心差异** | 智能路由/负载均衡/故障转移 | API 生命周期管理/订阅审批 |
| **用户体系** | User（单租户） | Team + Member + Role (多租户) |
| **计费模式** | Token 限额（用户级别） | 流量配额管理 |
| **路由策略** | RouteGroup（全局负载均衡/故障转移） | Channel（团队隔离） |
| **数据库架构** | 共享 Schema，全局 Provider/RouteGroup | 共享 Schema + team_id 行级隔离 |
| **服务类型** | AI Service（OpenAI + Anthropic 双标准） | AI Service + REST Service |

### 1.2 实体域划分

根据业务领域，将实体划分为以下四大域：

```
┌─────────────────────────────────────────────────────────────┐
│                      实体域划分                              │
├─────────────────────────────────────────────────────────────┤
│  ① 身份与访问控制域    │  User、Role、Permission、Member     │
│  ② 提供商与模型域      │  Provider、Model、ProviderApiKey   │
│  ③ 路由与负载均衡域    │  RouteGroup、RouteGroupProvider     │
│  ④ 令牌与限额域        │  GatewayApiKey、TokenLimit          │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 实体清单总览

| 域 | 实体数 | 实体列表 |
|---|--------|----------|
| ① 身份与访问控制 | 4 | User、Role、Permission、Member |
| ② 提供商与模型 | 3 | Provider、Model、ProviderApiKey |
| ③ 路由与负载均衡 | 2 | RouteGroup、RouteGroupProvider |
| ④ 令牌与限额 | 2 | GatewayApiKey、TokenLimit |
| **合计** | **11** | |

---

## 二、核心实体详解

### 2.1 身份与访问控制域

#### 2.1.1 User（用户）

**定义**: 系统用户实体，支持多角色绑定。用户通过 OAuth 或用户名密码认证。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| user_code | VARCHAR(64) | 用户编码 | UNIQUE, NOT NULL |
| username | VARCHAR(64) | 用户名 | NOT NULL |
| email | VARCHAR(128) | 邮箱 | UNIQUE, NOT NULL |
| password_hash | VARCHAR(256) | 密码哈希 | BCrypt 加密，cost≥12 |
| phone | VARCHAR(32) | 手机号 | NULL |
| avatar_url | VARCHAR(512) | 头像URL | NULL |
| status | ENUM | 状态 | ACTIVE / DISABLED / LOCKED / DELETED |
| email_verified | BOOLEAN | 邮箱已验证 | DEFAULT false |
| oauth_providers | JSON | OAuth提供者列表 | GitHub / Gitee / LinuxDO |
| pii_salt | VARCHAR(64) | PII脱敏盐值 | GDPR 删除权用 |
| last_login_at | TIMESTAMP | 最后登录时间 | NULL |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |
| deleted_at | TIMESTAMP | 删除时间 | NULL |

**业务说明**: LLM-Gateway 采用单租户架构，所有用户共享全局的 Provider、RouteGroup 等资源。

#### 2.1.2 Member（成员）

**定义**: 用户与角色的多对多关联表，记录用户所拥有的角色。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| user_id | BIGINT | 用户ID | FK → User.id, NOT NULL, INDEX |
| role_id | BIGINT | 角色ID | FK → Role.id, NOT NULL, INDEX |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |

**唯一约束**: `(user_id, role_id)` 联合唯一。

#### 2.1.3 Role（角色）

**定义**: 权限集合，用于简化权限管理和批量授权。角色分为 SYSTEM（预定义）和 CUSTOM（自定义）两种类型。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| role_code | VARCHAR(64) | 角色编码 | UNIQUE, NOT NULL |
| name | VARCHAR(64) | 角色名称 | NOT NULL |
| description | TEXT | 角色描述 | NULL |
| role_type | ENUM | 角色类型 | SYSTEM / CUSTOM |
| scope_type | ENUM | 权限范围 | SYSTEM（预留） |
| is_active | BOOLEAN | 是否启用 | DEFAULT true |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |
| deleted_at | TIMESTAMP | 删除时间 | NULL |

**预设角色**（SYSTEM 类型）:

| 角色 | 编码 | 说明 |
|------|------|------|
| 管理员 | ADMIN | 系统全部权限 |
| 开发者 | DEVELOPER | 创建令牌、查看日志、调用API |
| 观察者 | OBSERVER | 仅查看用量和日志 |
| 财务管理员 | FINANCE_ADMIN | 预算配置、费用查看 |

#### 2.1.4 Permission（权限）

**定义**: 细粒度权限码，定义到具体的操作级别。权限是 RBAC 系统的最小授权单元。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| permission_code | VARCHAR(128) | 权限编码 | UNIQUE, NOT NULL |
| name | VARCHAR(64) | 权限名称 | NOT NULL |
| description | TEXT | 权限描述 | NULL |
| category | VARCHAR(32) | 权限分类 | user / provider / model / token / log / setting |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |

**权限编码规范**: `resource:action` 格式。

示例权限码:
```
user:create    user:read    user:update    user:delete
provider:create  provider:read  provider:update  provider:delete
model:create    model:read    model:update    model:delete
token:create    token:read    token:revoke
log:read        audit:read
setting:read    setting:update
```

---

### 2.2 提供商与模型域

#### 2.2.1 Provider（提供商）

**定义**: 模型服务提供方，如 OpenAI、Anthropic、通义千问等。提供商是模型的上层分类单位，**全局共享**。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| provider_code | VARCHAR(64) | 业务标识 | UNIQUE, NOT NULL |
| provider_name | VARCHAR(128) | 显示名称 | NOT NULL |
| provider_type | ENUM | 类型 | OPENAI / ANTHROPIC / GEMINI / ZHIPU / OTHER |
| base_url | VARCHAR(256) | API 端点 | NOT NULL |
| website_url | VARCHAR(512) | 官网 URL | NULL |
| api_doc_url | VARCHAR(512) | API 文档 URL | NULL |
| priority | INT | 优先级 | DEFAULT 100，数值越大越优先 |
| status | ENUM | 状态 | ACTIVE / SUSPENDED / DELETED |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**业务规则**:
- `provider_type` 决定使用哪个 Adapter 实现类
- `priority` 用于路由时的默认排序
- Provider 是**全局**的，所有用户共享

#### 2.2.2 Model（模型）

**定义**: 具体的 AI 模型，是调用的最小单位。模型通过 Provider 归属提供商。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| model_code | VARCHAR(128) | 业务标识 | UNIQUE, NOT NULL |
| provider_id | BIGINT | 所属 Provider | FK → providers.id, NOT NULL |
| provider_model_id | VARCHAR(128) | Provider 侧模型 ID | NOT NULL（如 gpt-4o） |
| display_name | VARCHAR(256) | 显示名称 | NOT NULL |
| context_window | INT | 上下文窗口（token 数） | NULL 表示未知 |
| input_price | DECIMAL(10,6) | 输入价格（每 1M tokens） | NULL 表示不可用 |
| output_price | DECIMAL(10,6) | 输出价格（每 1M tokens） | NULL 表示不可用 |
| capabilities | JSON | 能力标志 | 支持 streaming/function_calling 等 |
| status | ENUM | 状态 | ACTIVE / DEPRECATED / DELETED |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**唯一约束**: `(provider_id, provider_model_id)` 联合唯一。

**capabilities JSON 示例**:
```json
{
  "streaming": true,
  "function_calling": true,
  "vision": false,
  "json_output": true
}
```

#### 2.2.3 ProviderApiKey（Provider 调用凭证）

**定义**: 网关调用大模型 Provider 时的凭据。**管理员配置，系统级别**。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| key_code | VARCHAR(64) | 业务标识 | UNIQUE, NOT NULL |
| provider_id | BIGINT | 所属 Provider | FK → providers.id, NOT NULL |
| key_name | VARCHAR(64) | Key 名称（如"主Key"） | NULL |
| api_key | VARCHAR(512) | API Key（加密存储） | NOT NULL |
| priority | INT | 优先级（用于轮换） | DEFAULT 100，数值越大越优先 |
| status | ENUM | 状态 | ACTIVE / DISABLED / EXHAUSTED / EXPIRED / DELETED |
| last_used_at | TIMESTAMP | 最后使用时间 | NULL |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**业务规则**:
- `api_key` 使用 AES-256 加密存储（由 security 模块提供）
- 同一 Provider 下可有多个 Key（主备/轮换）
- `status = EXHAUSTED` 时进入冷却期，不会被选择
- `status = DISABLED` 不会被选择，直到管理员恢复

**索引**:
- `idx_provider_id`: 用于查找某 Provider 下所有 Key
- `idx_provider_status`: 用于查找某 Provider 下可用的 Key

---

### 2.3 路由与负载均衡域

#### 2.3.1 RouteGroup（路由分组）

**定义**: 路由分组，用于负载均衡和故障转移。**全局共享**，支持三种路由策略。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| group_code | VARCHAR(64) | 业务标识 | UNIQUE, NOT NULL |
| group_name | VARCHAR(128) | 显示名称 | NOT NULL |
| strategy | ENUM | 路由策略 | ROUND_ROBIN / LEAST_LATENCY / PRIORITY |
| failover_enabled | BOOLEAN | 是否启用故障转移 | DEFAULT TRUE |
| max_retry | INT | 最大重试次数 | DEFAULT 2 |
| health_check_interval | INT | 健康检查间隔（秒） | DEFAULT 30 |
| description | TEXT | 描述 | NULL |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**路由策略说明**:

| 策略 | 说明 |
|------|------|
| `ROUND_ROBIN` | 按 weight 加权轮询 |
| `LEAST_LATENCY` | 选择延迟最低的 Provider |
| `PRIORITY` | 优先使用高优先级，失败后 failover |

#### 2.3.2 RouteGroupProvider（路由分组与Provider关联）

**定义**: 路由分组与 Provider 的多对多关联，支持权重、健康状态和优先级管理。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| route_group_id | BIGINT | 路由分组 | FK → route_groups.id, NOT NULL |
| provider_id | BIGINT | Provider | FK → providers.id, NOT NULL |
| weight | INT | 权重（用于负载均衡） | DEFAULT 100 |
| priority | INT | 优先级（用于故障转移） | DEFAULT 100，数值越大越优先 |
| status | ENUM | 状态 | ENABLED / DISABLED / UNHEALTHY |
| health_status | ENUM | 健康状态 | HEALTHY / DEGRADED / UNHEALTHY |
| consecutive_failures | INT | 连续失败次数 | DEFAULT 0 |
| last_health_check_at | TIMESTAMP | 最后健康检查时间 | NULL |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**唯一约束**: `(route_group_id, provider_id)` 联合唯一。

**状态说明**:

| status | 说明 |
|--------|------|
| ENABLED | 可用于路由 |
| DISABLED | 管理员禁用 |
| UNHEALTHY | 健康检查失败，自动禁用 |

| health_status | 说明 |
|---------------|------|
| HEALTHY | 正常 |
| DEGRADED | 部分失败，响应慢 |
| UNHEALTHY | 连续失败，不可用 |

---

### 2.4 令牌与限额域

#### 2.4.1 GatewayApiKey（网关访问凭证）

**定义**: 用户调用 LLM-Gateway 网关的凭据，格式为 `sk-xxxxxxxxxxxxxxxx`。**用户自行管理**。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| key_code | VARCHAR(128) | 业务标识 | UNIQUE, NOT NULL |
| key_hash | VARCHAR(256) | API Key 哈希（用于验证） | NOT NULL |
| user_id | BIGINT | 所属用户 | FK → users.id, NOT NULL |
| provider_id | BIGINT | 关联的 Provider | FK → providers.id, NULL 表示全部 |
| route_group_id | BIGINT | 路由分组 | FK → route_groups.id, NULL 表示默认 |
| name | VARCHAR(64) | 密钥名称 | NULL |
| status | ENUM | 状态 | ACTIVE / DISABLED / EXPIRED / DELETED |
| expires_at | TIMESTAMP | 过期时间 | NULL 表示永不过期 |
| last_used_at | TIMESTAMP | 最后使用时间 | NULL |
| model_whitelist | JSON | 允许使用的模型列表 | NULL 表示全部允许 |
| ip_whitelist | JSON | IP 白名单（支持 CIDR） | NULL 表示全部允许 |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**业务规则**:
- `key_hash` 用于验证 API 调用时传入的 Key
- `provider_id` 为 NULL 表示可访问所有 Provider
- `route_group_id` 为 NULL 使用系统默认路由策略
- 同一用户可创建多个 Key（主备/轮换）

**索引**:
- `idx_key_hash`: 用于 API 调用时快速查找
- `idx_user_provider`: 用于查询某用户的某 Provider 下所有 Key

#### 2.4.2 TokenLimit（Token 限额）

**定义**: 用户级别 Token 使用限额，支持周期重置和超限动作。

| 属性 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | BIGINT | 主键 | PK, AUTO_INCREMENT |
| limit_code | VARCHAR(64) | 业务标识 | UNIQUE, NOT NULL |
| user_id | BIGINT | 所属用户 | FK → users.id, NOT NULL |
| provider_id | BIGINT | 关联的 Provider | FK → providers.id, NULL 表示全部 |
| model_id | BIGINT | 关联的 Model | FK → models.id, NULL 表示全部 |
| token_limit_enabled | BOOLEAN | 是否启用 Token 限额 | DEFAULT TRUE |
| max_tokens | DECIMAL(20,6) | Token 限额总量 | NULL 表示不限 |
| used_tokens | DECIMAL(20,6) | 已用 Token 量 | DEFAULT 0 |
| request_limit_enabled | BOOLEAN | 是否启用请求次数限额 | DEFAULT FALSE |
| max_requests | INT | 请求次数限额 | NULL |
| used_requests | INT | 已用请求次数 | DEFAULT 0 |
| period_type | ENUM | 周期类型 | DAILY / WEEKLY / MONTHLY / TOTAL |
| period_day_of_week | INT | 周内日期 | 1-7, WEEKLY 时有效 |
| period_day_of_month | INT | 月内日期 | 1-31, MONTHLY 时有效 |
| exceeded_action | ENUM | 超限动作 | REJECT / DOWNGRADE |
| switch_model_id | BIGINT | 降级切换模型 | NULL |
| status | ENUM | 状态 | ACTIVE / SUSPENDED / DELETED |
| created_by | BIGINT | 创建人 | FK → users.id |
| created_at | TIMESTAMP | 创建时间 | NOT NULL |
| updated_by | BIGINT | 更新人 | FK → users.id |
| updated_at | TIMESTAMP | 更新时间 | NOT NULL |

**业务规则**:
- `provider_id` 为 NULL 表示限制适用于所有 Provider
- `model_id` 为 NULL 表示限制适用于所有模型
- `period_type = TOTAL` 时不重置，累加总量
- `exceeded_action = DOWNGRADE` 时会切换到 `switch_model_id`

**周期重置说明**:

| period_type | 重置时机 |
|------------|----------|
| DAILY | 每天 UTC 00:00 重置 |
| WEEKLY | 每周一 UTC 00:00 重置 |
| MONTHLY | 每月 1 日 UTC 00:00 重置 |
| TOTAL | 不重置，累加总量 |

---

## 三、实体关系图 (ER Diagram)

### 3.1 核心关系速查表

| 关系 | 类型 | 说明 |
|------|------|------|
| User (1) : Member (N) | 一对多 | 用户拥有多个角色绑定 |
| Role (1) : Member (N) | 一对多 | 角色被多个用户使用 |
| User (1) : GatewayApiKey (N) | 一对多 | 用户拥有多个网关访问凭证 |
| User (1) : TokenLimit (N) | 一对多 | 用户可设置多个 Token 限额 |
| Provider (1) : Model (N) | 一对多 | 提供商提供多个模型 |
| Provider (1) : ProviderApiKey (N) | 一对多 | 提供商可有多个调用凭证 |
| Provider (1) : RouteGroupProvider (N) | 一对多 | 提供商可属于多个路由分组 |
| RouteGroup (1) : RouteGroupProvider (N) | 一对多 | 路由分组包含多个 Provider 关联 |
| RouteGroupProvider (N) : Provider (1) | 多对一 | 关联的 Provider |
| GatewayApiKey (N) : Provider (1) | 多对一 | 可选限定 Provider |
| GatewayApiKey (N) : RouteGroup (1) | 多对一 | 可选指定路由分组 |
| TokenLimit (N) : Provider (1) | 多对一 | 可选限定 Provider |
| TokenLimit (N) : Model (1) | 多对一 | 可选限定模型 |

### 3.2 实体关系图（分域展示）

#### 3.2.1 身份与访问控制域

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                        身份与访问控制域                                       │
│                                                                              │
│  ┌─────────┐ 1     N:M      ┌──────────┐ 1     N:M      ┌──────────┐        │
│  │  User   │────────────────│  Member   │────────────────│   Role   │        │
│  │   用户   │                │   成员    │                │   角色   │        │
│  └────┬────┘                └─────┬─────┘                └────┬─────┘        │
│       │ N:1                       │                           │              │
│       ▼                           ▼                           │              │
│  ┌──────────┐                ┌──────────┐ 1:N  ┌──────────┐  │              │
│  │GatewayApi│                │MemberRole│──────│Permission│──┘              │
│  │Key       │                └──────────┘      │   权限   │                 │
│  │网关访问凭证│                                        └──────────┘                 │
│  └──────────┘                                                                  │
│       │                                                                       │
│       │ 1:N                                                                   │
│       ▼                                                                       │
│  ┌──────────┐                                                                  │
│  │TokenLimit│                                                                  │
│  │Token限额  │                                                                  │
│  └──────────┘                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘
```

#### 3.2.2 提供商与模型域

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          提供商与模型域                                        │
│                                                                              │
│   ┌────────────┐ 1:N ┌─────────┐                                           │
│   │  Provider  │─────│  Model   │                                           │
│   │   提供商    │     │   模型    │                                           │
│   └──────┬─────┘     └─────────┘                                           │
│          │                                                                │
│          │ 1:N                                                           │
│          ▼                                                                │
│   ┌────────────┐                                                        │
│   │ProviderApi │                                                        │
│   │Key         │                                                        │
│   │调用凭证     │                                                        │
│   └────────────┘                                                        │
│                                                                        │
│   Provider ──── 1:N ──── RouteGroupProvider                            │
└──────────────────────────────────────────────────────────────────────────────┘
```

#### 3.2.3 路由与负载均衡域

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          路由与负载均衡域                                     │
│                                                                              │
│   ┌────────────────┐ 1:N ┌──────────────────────┐ N:1 ┌────────────┐       │
│   │   RouteGroup   │─────│ RouteGroupProvider   │─────│  Provider  │       │
│   │    路由分组     │     │   路由分组与Provider关联 │     │   提供商    │       │
│   └────────────────┘     └──────────────────────┘     └────────────┘       │
│                                      │                                       │
│                                      │ N:1                                   │
│                                      ▼                                       │
│                              ┌────────────┐                                │
│                              │  Provider  │                                │
│                              │   提供商    │                                │
│                              └────────────┘                                │
│                                                                              │
│   RouteGroup.strategy: ROUND_ROBIN / LEAST_LATENCY / PRIORITY              │
│   RouteGroup.failover_enabled: 启用故障转移时，失败后自动切换                 │
└──────────────────────────────────────────────────────────────────────────────┘
```

#### 3.2.4 令牌与限额域

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           令牌与限额域                                         │
│                                                                              │
│   User (用户)                                                                │
│   ├── 1:N ── GatewayApiKey (网关访问凭证)                                     │
│   │         ├── key_hash: 用于验证 API 调用                                   │
│   │         ├── provider_id: 可选限定 Provider                                │
│   │         ├── route_group_id: 可选指定路由分组                              │
│   │         └── model_whitelist / ip_whitelist: 访问控制                     │
│   │                                                                         │
│   └── 1:N ── TokenLimit (Token 限额)                                        │
│             ├── provider_id: 可选限定 Provider                               │
│             ├── model_id: 可选限定模型                                       │
│             ├── max_tokens / used_tokens: 限额管理                           │
│             ├── period_type: DAILY / WEEKLY / MONTHLY / TOTAL               │
│             └── exceeded_action: REJECT / DOWNGRADE                          │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、路由流程

### 4.1 请求路由流程

```
用户请求
    │
    ▼
┌─────────────────────┐
│  GatewayApiKey 认证  │ ← 验证 key_hash
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│   确定 Provider     │ ← 三种方式：
│                     │   1. 请求指定 provider_id
│                     │   2. 请求指定 model → Model.providerId
│                     │   3. 使用 RouteGroup 策略
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│  RouteGroup 策略    │ ← 加权轮询/最小延迟/优先级
│  选择最终 Provider  │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ ProviderApiKey 调用 │ ← 选择优先级最高的可用 Key
└─────────────────────┘
```

### 4.2 故障转移流程

```
请求到达
    │
    ▼
选择 Provider A (优先级最高)
    │
    ▼
调用 Provider A → 失败 (超时/限流/错误)
    │
    ▼
检查 RouteGroup.failoverEnabled = true ?
    │
    ├── 否 → 返回错误
    │
    ▼
查找下一个可用 Provider (status=ENABLED)
    │
    ▼
重新尝试调用 → 成功
    │
    ▼
返回结果（记录 failover 事件）
```

---

## 五、状态流转

### 5.1 Provider 状态流转

```
ACTIVE ──→ SUSPENDED（管理员暂停）
ACTIVE ──→ DELETED（软删除）
SUSPENDED ──→ ACTIVE（恢复使用）
```

### 5.2 ProviderApiKey 状态流转

```
ACTIVE ──→ DISABLED（管理员禁用）
ACTIVE ──→ EXHAUSTED（限流触发）
ACTIVE ──→ EXPIRED（过期）
EXHAUSTED ──→ ACTIVE（冷却后恢复）
DISABLED ──→ ACTIVE（恢复使用）
EXPIRED ──→ ACTIVE（续期后恢复）
```

### 5.3 RouteGroupProvider 状态流转

```
ENABLED ──→ DISABLED（管理员禁用）
ENABLED ──→ UNHEALTHY（健康检查失败）
UNHEALTHY ──→ ENABLED（健康检查恢复）
DISABLED ──→ ENABLED（恢复使用）
```

### 5.4 GatewayApiKey 状态流转

```
ACTIVE ──→ DISABLED（管理员禁用）
ACTIVE ──→ EXPIRED（过期）
DISABLED ──→ ACTIVE（恢复使用）
EXPIRED ──→ ACTIVE（续期后恢复）
```

---

## 六、附录

### 6.1 实体属性规范

#### 审计字段（所有业务表必须包含）

```sql
created_by    BIGINT       -- 创建人ID (FK → users.id, 系统生成填 0L)
created_at    TIMESTAMP    -- 创建时间 (NOT NULL, DEFAULT CURRENT_TIMESTAMP)
updated_by    BIGINT       -- 更新人ID
updated_at    TIMESTAMP    -- 更新时间 (NOT NULL, ON UPDATE CURRENT_TIMESTAMP)
```

#### 时间戳规范

| 字段类型 | Java 类型 | 数据库类型 | 说明 |
|---------|----------|-----------|------|
| 创建时间 | Instant | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| 更新时间 | Instant | TIMESTAMP | NOT NULL, ON UPDATE CURRENT_TIMESTAMP |
| 删除时间 | Instant | TIMESTAMP NULL | 软删除标记 |
| 过期时间 | Instant | TIMESTAMP NULL | 永不过期填 NULL |

#### 状态枚举规范

| 实体 | 状态值 |
|------|--------|
| User | ACTIVE, DISABLED, LOCKED, DELETED |
| Provider | ACTIVE, SUSPENDED, DELETED |
| ProviderApiKey | ACTIVE, DISABLED, EXHAUSTED, EXPIRED, DELETED |
| Model | ACTIVE, DEPRECATED, DELETED |
| RouteGroup | （无状态字段） |
| RouteGroupProvider | ENABLED, DISABLED, UNHEALTHY |
| RouteGroupProvider.health_status | HEALTHY, DEGRADED, UNHEALTHY |
| GatewayApiKey | ACTIVE, DISABLED, EXPIRED, DELETED |
| TokenLimit | ACTIVE, SUSPENDED, DELETED |

### 6.2 索引命名规范

```sql
-- 主键索引
pk_{table_name}

-- 唯一索引
uk_{table_name}_{column_list_underscored}

-- 普通索引
idx_{table_name}_{column_list_underscored}

-- 示例
pk_provider                  -- Provider 主键
uk_provider_code              -- Provider 编码唯一
uk_gateway_api_key_code       -- GatewayApiKey 编码唯一
idx_provider_api_key_provider -- ProviderApiKey Provider+状态索引
```

### 6.3 版本信息

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-04-18 | 初始版本，整合 VoAPI 和 LLM-Gateway spec |
| v2.0 | 2026-04-18 | 评审修订：修复设计不一致问题、补充缺失实体、优化 ER 图 |
| v3.0 | 2026-04-18 | 整合 APIPark 实体设计，移除 VoAPI 引用 |
| v4.0 | 2026-04-18 | 删除 Tenant/Project，采用 APIPark Team 作为唯一隔离边界 |
| v4.1 | 2026-04-18 | 修复章节编号、移除过时实体引用、更新 ER 图和隔离设计 |
| v5.0 | 2026-04-23 | 单租户架构重构：移除 Team/Member/Channel/ChannelKey，新增 RouteGroup/RouteGroupProvider/GatewayApiKey |
