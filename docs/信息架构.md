# 信息架构 / Information Architecture

> **文档版本**: 3.0
> **项目版本**: v1.0.0
> **生成日期**: 2026-05-01
> **状态**: 草案

---

## 目录

- [概述](#概述)
- [业务域总览](#业务域总览)
- [核心业务链路](#核心业务链路)
- [概念定义](#概念定义)
- [1. proxy — 模型代理（核心域）](#1-proxy--模型代理核心域)
- [2. provider — 模型广场](#2-provider--模型广场)
- [3. access — 访问控制](#3-access--访问控制)
- [4. quota — 用量管控](#4-quota--用量管控)
- [5. audit — 审计追溯](#5-audit--审计追溯)
- [6. alert — 告警通知](#6-alert--告警通知)
- [跨域关系总览](#跨域关系总览)
- [变更记录](#变更记录)

---

## 概述

本文档定义 LLM-Gateway 的业务概念和信息模型，为后续数据架构设计提供业务语义基础。

### 网关定位

LLM-Gateway 是大模型调用的**反向代理层**，本质职责：

- **代理转发**：将请求准确传递到目标 Provider
- **策略管控**：认证、限流、限额、审计

### 设计原则

| 原则 | 说明 |
|------|------|
| **网关不存储对话** | 不保存对话内容，不维护会话状态，不解析消息语义 |
| **统一入口** | 对外暴露 OpenAI 兼容端点，屏蔽下游 Provider 差异 |
| **策略集中管控** | 认证、限额、审计等策略在网关层统一执行 |

### 划域方法

按**业务能力**划分业务域——每个域回答一个核心问题，拥有独立的实体和生命周期。

---

## 业务域总览

| # | 业务能力 | 英文域 | 核心问题 | 定位 |
|---|---------|--------|---------|------|
| 1 | 模型代理 | **proxy** | 怎么调模型？ | **核心域** |
| 2 | 模型广场 | **provider** | 有哪些可用？ | 支撑域 |
| 3 | 访问控制 | **access** | 谁能调？ | 支撑域 |
| 4 | 用量管控 | **quota** | 能调多少？ | 支撑域 |
| 5 | 审计追溯 | **audit** | 谁做过什么？ | 支撑域 |
| 6 | 告警通知 | **alert** | 出了什么问题？ | 支撑域 |

**域定位说明**：
- **核心域**：网关差异化竞争力所在，缺失则失去存在价值
- **支撑域**：企业级客户刚需，为核心域提供保障机制

---

## 核心业务链路

```
请求 → access(认证鉴权) → quota(限额限流) → proxy(查provider路由转发) → quota(计量) → audit(记录) → 响应
                                                              ↓
                                                        alert(异常告警)
```

**请求处理流程**：

1. **access**：验证调用者身份，判断是否有权限访问
2. **quota**：检查 Token 额度、RPM/TPM 限制
3. **proxy**：从 provider 域获取可用渠道，执行路由决策、协议转换、请求转发
4. **quota**：记录实际 Token 消耗
5. **audit**：记录调用日志，满足审计需求
6. **alert**：异常情况触发告警通知

---

## 概念定义

### 核心概念

| 概念 | 英文名 | 定义 | 归属域 |
|------|--------|------|--------|
| 路由分组 | RouteGroup | 一组路由策略配置，支持负载均衡和故障转移 | proxy |
| 路由关联 | RouteGroupChannel | 路由分组与 Channel 的关联，含权重/优先级 | proxy |
| 模型提供商 | Provider | AI 模型服务提供方（如 OpenAI、Anthropic） | provider |
| 渠道 | Channel | Provider 的具体调用实例，含 BaseURL、API Key、优先级 | provider |
| AI 模型 | Model | 具体的大模型（如 gpt-4o、claude-sonnet-4） | provider |
| 提供商凭证 | ProviderApiKey | 网关调用 Provider 的 API 密钥，支持多 Key 轮换 | provider |
| 系统用户 | User | 网关使用者，包含管理员和普通用户角色 | access |
| 角色 | Role | 权限集合，支持系统预设和自定义 | access |
| 权限 | Permission | 细粒度操作权限码 | access |
| 网关凭证 | GatewayApiKey | 用户调用网关的凭证（sk-xxx 格式） | access |
| IP 黑名单 | IpBlocklist | 被系统屏蔽的 IP 地址列表 | access |
| 敏感数据规则 | SensitiveDataRule | PII 检测与脱敏规则定义 | access |
| Token 限额 | TokenLimit | 用户/凭证的 Token 用量上限配置 | quota |
| 限流配置 | RateLimitConfig | RPM/TPM 请求次数限制配置 | quota |
| 用量记录 | UsageRecord | API 调用消耗的 Token 统计记录 | quota |
| 审计日志 | AuditLog | 管理操作记录，满足合规要求 | audit |
| 调用日志 | CallLog | API 调用元数据记录（用于追溯） | audit |
| 告警规则 | AlertRule | 告警触发条件定义 | alert |
| 告警模板 | AlertTemplate | 告警通知格式模板 | alert |
| 告警通知 | AlertNotification | 告警发送记录 | alert |

### 概念关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              业务概念关系                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [access]                                                                   │
│  User ──持有──► GatewayApiKey ──拥有──► Role ──包含──► Permission          │
│     │                                                                       │
│     └──产生──► AuditLog                                                     │
│                                                                             │
│  [quota]                                                                    │
│  GatewayApiKey ──受限于──► TokenLimit                                       │
│                 ──受限于──► RateLimitConfig                                 │
│                 ──产生──► UsageRecord                                        │
│                                                                             │
│  [proxy]                                                                    │
│  RouteGroup ──包含──► RouteGroupChannel ──关联──► Channel                   │
│                                                       │                     │
│                                                       ▼                     │
│  [provider]                                         Model                    │
│  Provider ──提供──► Model                            │                      │
│          └──配置──► Channel ──使用──► ProviderApiKey ─┘                     │
│                                                                             │
│  [audit]                                                                    │
│  CallLog ──触发──► AlertRule ──使用──► AlertTemplate                        │
│                    │                                                        │
│                    ▼                                                        │
│             AlertNotification                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 1. proxy — 模型代理（核心域）

### 责任

回答"**怎么调模型？**"

以标准协议接收请求，选择最优上游渠道，完成调用并返回。

### 子能力

| 子能力 | 说明 |
|--------|------|
| 协议适配 | 接收 OpenAI/Anthropic 格式请求，验证参数 |
| 路由决策 | 根据策略选择目标 Channel/Model |
| 协议转换 | OpenAI ↔ Anthropic 格式互转 |
| 负载均衡 | 多 Channel 按权重分配流量 |
| 故障转移 | 主 Channel 失败自动切换备用 |
| 熔断重试 | 连续失败时熔断，超时/错误自动重试 |
| 流式传输 | SSE 流式响应的分块传输 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| RouteGroup | 路由分组 | 一组路由策略配置，支持负载均衡和故障转移 |
| RouteGroupChannel | 路由关联 | 路由分组与 Channel 的关联，含权重/优先级/健康状态 |

### 实体关系

```
RouteGroup
├── 聚合 RouteGroupChannel（一个分组包含多个路由关联）
├── 配置负载均衡策略（权重分配）
├── 配置故障转移策略（备用 Channel）
└── 被调用请求引用

RouteGroupChannel
├── 关联 Channel（一个路由关联指向一个渠道）
├── 持有权重配置（用于负载均衡流量分配）
├── 持有优先级配置（高优先级优先使用）
├── 持有健康状态（由健康检查更新）
└── 当 Channel 不可用时自动跳过
```

---

## 2. provider — 模型广场

### 责任

回答"**有哪些可用？**"

管理上游模型提供商、渠道、模型及其凭证。

### 子能力

| 子能力 | 说明 |
|--------|------|
| Provider 管理 | 模型提供商（OpenAI、Anthropic 等） |
| Channel 管理 | 渠道实例（一个 Provider 可有多个 Channel） |
| Model 管理 | 模型列表和元数据 |
| ProviderApiKey 管理 | Provider 调用凭证，支持多 Key 轮换 |
| 健康检查 | 定期检测 Channel 可用性 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| Provider | 模型提供商 | 模型服务提供方，如 OpenAI、Anthropic |
| Channel | 渠道 | Provider 的具体调用实例，含 BaseURL、API Key、优先级、权重 |
| Model | AI 模型 | 具体的大模型，如 gpt-4o、claude-sonnet-4 |
| ProviderApiKey | Provider 凭证 | 网关调用 Provider 的凭据，管理员配置，支持多 Key 轮换 |

### 实体关系

```
Provider
├── 提供多个 Model（一个 Provider 下有多个可用模型）
├── 配置多个 Channel（一个 Provider 可有多个调用实例）
├── 标识官网、API 文档链接
└── 由管理员创建/启用/禁用

Channel
├── 归属一个 Provider（渠道不能独立存在）
├── 配置 BaseURL（可使用代理地址）
├── 关联多个 ProviderApiKey（支持多 Key 轮换）
├── 持有优先级/权重（用于路由决策）
├── 持有健康状态（由健康检查更新）
└── 可被 RouteGroupChannel 引用

Model
├── 必须归属一个 Provider（模型不能独立存在）
├── 标识模型能力（上下文长度、多模态、Tool Use 支持）
└── 可被路由关联引用

ProviderApiKey
├── 用于访问一个 Channel（凭证绑定到特定渠道）
├── 支持多 Key 轮换（自动选择可用 Key）
├── 加密存储
├── 持有 RPM/TPM 限制（上游 Provider 限制）
└── 失效时由健康检查自动禁用
```

### Provider vs Channel

```
Provider（抽象提供商）        Channel（具体渠道实例）
─────────────────           ────────────────────
OpenAI                      渠道1: OpenAI 官方 (key-1, 优先级100)
                            渠道2: OpenAI 代理 (key-2, 优先级50, base_url=代理地址)
Anthropic                   渠道3: Anthropic 官方 (key-3)
```

一个 Provider 可以有多个 Channel，每个 Channel 有独立的 BaseURL、API Key、优先级、权重。这正是**负载均衡和故障转移**的前提。

---

## 3. access — 访问控制

### 责任

回答"**谁能调？**"

管理调用者身份、凭证、权限，控制谁能以什么条件访问哪些资源。

### 子能力

| 子能力 | 说明 |
|--------|------|
| 身份认证 | 验证请求者身份（Bearer Token） |
| 角色权限 | 管理员/普通用户权限判断（RBAC） |
| 凭证管理 | 网关调用凭证的创建、过期、撤销 |
| IP 过滤 | 黑名单/白名单控制 |
| 敏感数据保护 | PII 检测与脱敏 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| User | 系统用户 | 网关使用者，含角色关联 |
| Role | 角色 | 权限集合，支持系统预设和自定义 |
| Permission | 权限 | 细粒度操作权限码 |
| UserRole | 用户角色关联 | 用户与角色的多对多关系 |
| GatewayApiKey | 网关凭证 | 用户调用网关的凭证，用户自管理 |
| IpBlocklist | IP 黑名单 | 系统屏蔽的 IP 地址 |
| SensitiveDataRule | 敏感数据规则 | PII 检测与脱敏规则 |

### 实体关系

```
User
├── 关联多个 Role（通过 UserRole）
├── 创建多个 GatewayApiKey（用户持有自己的凭证）
├── 操作产生 AuditLog（谁做了什么）
├── 登录触发身份认证
└── 密码 BCrypt 加密存储

Role
├── 类型：SYSTEM（系统预设）/ CUSTOM（自定义）
├── 包含多个 Permission（权限码）
├── 可被多个 User 关联
└── 支持软删除

Permission
├── 权限码格式：resource:action（如 provider:read, model:create）
├── 归属分类（provider、model、user、quota 等）
└── 被多个 Role 引用

GatewayApiKey
├── 被 User 创建（用户生成自己的 Key）
├── 用于请求认证（携带在请求头中）
├── 可设置 IP 白名单（限制访问来源）
├── 可设置模型白名单（限制可用模型）
├── 过期时间可配置
└── 哈希存储（不可还原）

IpBlocklist
├── 被系统全局应用（所有请求都检查）
├── 由管理员配置
├── 支持 IP/CIDR 格式
└── 命中时返回 403

SensitiveDataRule
├── 被应用到请求/响应进行脱敏
├── 包含检测规则（正则、关键词）
├── 定义脱敏方式（掩码、哈希、删除）
└── 覆盖 PII 类型：手机号、身份证、邮箱、银行卡等
```

### 预设角色

| 角色 | 权限范围 |
|------|---------|
| **管理员** | 系统全部权限 |
| **开发者** | 创建 API Key、查看日志、查看用量、调用 API |
| **观察者** | 仅查看用量和日志（只读权限） |
| **用量审计员** | 用量报表查看、Token 限额查看、无配置权限 |

---

## 4. quota — 用量管控

### 责任

回答"**能调多少？**"

计量每次调用的 Token 消耗，按层级限制用量，防止超额使用。

### 子能力

| 子能力 | 说明 |
|--------|------|
| Token 计量 | 分别统计输入/输出 Token |
| Token 限额 | 用户/Key/用户×渠道多级限制 |
| 请求限流 | RPM/TPM 令牌桶控制 |
| 预扣与结算 | 请求前预扣，实际消耗后调整 |
| 超限策略 | 拒绝/降级/切换 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| TokenLimit | Token 限额 | 用户/Key/渠道的 Token 用量上限 |
| RateLimitConfig | 限流配置 | RPM/TPM 请求次数限制 |
| UsageRecord | 用量记录 | API 调用消耗的 Token 统计记录 |

### 实体关系

```
TokenLimit
├── 约束 User（用户级限额）
├── 约束 GatewayApiKey（Key 级限额）
├── 可选约束 Channel（用户×渠道限额）
├── 超限时触发预设策略（REJECT → 402 / DOWNGRADE / SWITCH）
├── 支持周期重置（天/周/月/总量）
└── 预扣机制：请求前预估用量并冻结

RateLimitConfig
├── 约束 GatewayApiKey（Key 级 RPM/TPM）
├── 超限时返回 429 + Retry-After 头
├── 独立于 TokenLimit（两种不同的限制机制）
└── 支持 RPM（每分钟请求数）和 TPM（每分钟 Token 数）

UsageRecord
├── 归属 GatewayApiKey（记录 Key 的用量）
├── 记录输入 Token 数量
├── 记录输出 Token 数量
├── 用于限额计算和报表生成
├── 支持按时间聚合（天/周/月统计）
└── 由 proxy 域执行调用后产生
```

### 限额层级

```
TokenLimit 层级关系
├── 用户级: User（单用户总限额）
├── Key 级: GatewayApiKey（单个凭证限额）
└── 用户×渠道级: User × Channel（精细化配额控制，企业版）
```

---

## 5. audit — 审计追溯

### 责任

回答"**谁做过什么？**"

记录管理操作和 API 调用，满足合规审计需求。

### 子能力

| 子能力 | 说明 |
|--------|------|
| 操作审计 | 记录所有管理操作 |
| 调用记录 | 记录 API 调用元数据 |
| 不可篡改性 | 链式哈希，WORM 存储 |
| 合规报告 | 满足 SOX/GDPR/等保要求 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| AuditLog | 审计日志 | 管理操作记录 |
| CallLog | 调用日志 | API 调用元数据记录 |

### 实体关系

```
AuditLog
├── 记录操作者（User）
├── 记录操作类型（操作码）
├── 记录操作内容（变更前后数据）
├── 记录操作时间（时间戳）
├── 记录操作结果（成功/失败）
├── 记录客户端 IP（来源追溯）
├── 使用链式哈希（每条包含前一条哈希值）
├── WORM 存储（不可修改/删除）
└── 满足 SOX/GDPR/等保合规要求

CallLog
├── 归属 GatewayApiKey（记录凭证的调用）
├── 归属 User（记录用户的调用）
├── 记录调用元数据：
│   ├── Channel / Provider / Model
│   ├── 请求时间、响应时间
│   ├── 输入/输出 Token 数量
│   ├── 状态码、错误信息
│   └── Trace ID（追踪标识）
├── 用于用量统计和审计追溯
└── 由 proxy 域执行调用后产生
```

---

## 6. alert — 告警通知

### 责任

回答"**出了什么问题？**"

定义异常条件，发现问题后通知责任人。

### 子能力

| 子能力 | 说明 |
|--------|------|
| 规则定义 | 告警触发条件 |
| 模板管理 | 通知格式模板 |
| 通知发送 | 邮件、Webhook 等 |
| 历史记录 | 告警发送历史 |

### 实体

| 实体 | 英文名 | 说明 |
|------|--------|------|
| AlertRule | 告警规则 | 告警触发条件定义 |
| AlertTemplate | 告警模板 | 通知格式模板 |
| AlertNotification | 告警通知 | 告警发送记录 |

### 实体关系

```
AlertRule
├── 定义触发条件（阈值、表达式）
├── 关联 AlertTemplate（使用模板格式化通知）
├── 由领域事件触发（调用日志、用量数据、健康检查结果）
├── 触发后产生 AlertNotification
├── 可属于不同业务场景：
│   ├── 安全告警（登录异常、额度超限）
│   ├── 路由告警（Channel 不可用、失败率上升）
│   ├── 供给告警（Key 余额不足、模型下线）
│   └── 系统告警（服务异常、配置变更）
└── 支持静默期配置（避免重复告警）

AlertTemplate
├── 定义通知格式（标题、内容结构）
├── 被多个 AlertRule 引用（模板复用）
├── 支持多种渠道（邮件、Webhook、短信）
├── 包含变量占位符
└── 支持多语言（中文/英文）

AlertNotification
├── 由 AlertRule 触发产生
├── 使用 AlertTemplate 格式化
├── 记录：
│   ├── 触发时间
│   ├── 告警内容
│   ├── 通知渠道
│   ├── 发送状态（成功/失败）
│   └── 确认状态（已确认/未确认）
└── 可被用户确认（Ack）
```

### 告警场景

| 场景 | 触发条件 | 级别 |
|------|---------|------|
| Channel 失败率 | >5% (5 分钟内) | WARN |
| Channel 失败率 | >20% (5 分钟内) | ERROR |
| Token 限额使用率 | >80% | WARN |
| Token 限额耗尽 | ≥100% | ERROR |
| 平均延迟 | P95 > 500ms | WARN |

---

## 跨域关系总览

### 实体关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              跨域实体关系                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [access]                          [quota]                                  │
│  ┌─────────┐                      ┌──────────────┐                         │
│  │  User   │──────────────────►  │  TokenLimit  │                         │
│  └────┬────┘                      └──────────────┘                         │
│       │                           ┌──────────────┐                         │
│       ├───► GatewayApiKey ──────►│RateLimitConfig│                        │
│       │                           └──────────────┘                         │
│       │                                  │                                 │
│       │                                  ▼                                 │
│       │                           ┌──────────────┐                         │
│       └───► AuditLog              │ UsageRecord  │                         │
│                                     └──────────────┘                         │
│                                           ▲                                 │
│                                           │                                 │
│  [proxy]                                  │                                 │
│  ┌─────────────┐                         │                                 │
│  │ RouteGroup  │                         │                                 │
│  └──────┬──────┘                         │                                 │
│         │                                │                                 │
│         ▼                                │                                 │
│  ┌──────────────────┐                    │                                 │
│  │RouteGroupChannel │                    │                                 │
│  └────────┬─────────┘                    │                                 │
│           │                              │                                 │
│           ▼                              │                                 │
│  [provider]                              │                                 │
│  ┌──────────┐    ┌─────────┐             │                                 │
│  │ Provider │───►│ Channel │─────────────┼───► CallLog ──► [audit]        │
│  └────┬─────┘    └────┬────┘             │              │                  │
│       │               │                  │              ▼                  │
│       ▼               ▼                  │        ┌───────────┐            │
│  ┌─────────┐    ┌────────────────┐       │        │ AlertRule │──► [alert]│
│  │  Model  │    │ ProviderApiKey │───────┘        └───────────┘            │
│  └─────────┘    └────────────────┘                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 业务一致性约束

| 约束 | 说明 |
|------|------|
| RouteGroup 必须关联至少一个 RouteGroupChannel | 路由分组不能为空 |
| RouteGroupChannel 引用的 Channel 必须存在 | 路由关联必须指向有效渠道 |
| Channel 必须归属于一个 Provider | 渠道不能独立存在 |
| Model 必须归属于一个 Provider | 模型不能独立存在 |
| ProviderApiKey 必须关联一个 Channel | 凭证不能独立存在 |
| TokenLimit 必须指定限制类型和周期 | 限额不能无限制 |
| GatewayApiKey 必须归属于一个 User | 凭证不能是无主之物 |
| AuditLog.previous_hash 必须指向前一条记录 | 形成链式校验 |
| AlertRule 必须关联一个 AlertTemplate | 告警必须有格式化模板 |

### 错误分类

| 错误域 | 核心问题 | 错误类型 |
|--------|---------|---------|
| **proxy** | 代理转发失败 | Provider 调用超时、响应格式错误、连接失败 |
| **provider** | 资源不可用 | 无可用 Channel、所有 Channel 均不可用 |
| **access** | 访问被拒绝 | 认证失败、授权失败、IP 被屏蔽 |
| **quota** | 用量超限 | Token 限额超限、请求限流触发 |

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-04-29 | 初始版本 |
| v2.0 | 2026-04-29 | 撤销 session 域；UsageRecord 归入 quota 域；CallLog 归入 audit 域 |
| v2.7 | 2026-04-30 | 补充错误响应结构、跨域关系 |
| v3.0 | 2026-05-01 | **重构为 6 域模型**：router+proxy 合并为 proxy；supply 改名 provider；security 改名 access；引入 Channel 实体；补充 Role/Permission/UserRole 实体；更新核心业务链路 |
