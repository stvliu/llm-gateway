# 模型目录及时准确（models.dev 同步）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 models.dev 模型主数据源，通过手工触发同步完善 models 表字段（description/release_date/license/open_weights/benchmarks/weights/source/external_id/locked_fields），实现模型数据及时准确。

**Architecture:** 新增 provider 模块 `provider/catalog/sync` 子包：`ModelCatalogClient`（JDK HttpClient 拉取 models.dev/models.json）→ `ModelCatalogSyncService`（三阶段 upsert：按 external_id 匹配、跳过人工锁定字段、不删旧模型）→ `CatalogSyncController`（手工触发 + 状态查询）。models 表加 10 列，新增 `catalog_sync_logs` 同步日志表。审计由现有 `AuditLogInterceptor` 自动覆盖（`POST /api/v1/**` 管理操作）。

**Tech Stack:** Java 21、Spring Boot 3.5.x、Spring Data JPA（jsonb 字段用 `@JdbcTypeCode(SqlTypes.JSON)`）、JDK 内置 `java.net.http.HttpClient`（零新增依赖）、Jackson、JUnit 5 + Mockito + AssertJ、React（前端）。

## Global Constraints

- 遵守分层依赖：域接口定义在 provider 模块，JPA 实现在 provider-data 模块，web 组装在 gateway-web。
- 中文注释与 Javadoc：public 方法必须中文 Javadoc（CLAUDE.md 规范）。
- TDD：每个任务先写失败测试再实现。
- 提交信息用中文，逐步提交（每任务一次 commit）。
- 数据库方言兼容 H2（PostgreSQL 兼容模式）/ PostgreSQL：`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`、`CREATE TABLE IF NOT EXISTS`。
- 审计字段：`created_by/created_at/updated_by/updated_at`（继承 `BaseDo`/`BaseEntity`）。
- **不改名现有字段** contextWindow/maxInputTokens/maxOutputTokens（已评估：破坏性重构、牵涉 20 文件与 API 契约，本期不做）；同步映射层做字段名转换。
- 模型能力字段映射进现有 `capabilities` Map，不新增能力列。
- 不删除 models.dev 未出现的现有模型（避免破坏渠道/路由/限额）。
- 生产代码不得出现 TBD/TODO 占位。

---

### Task 1: V69 迁移 + Model/ModelDo 字段扩展 + Repository 扩展

**Files:**
- Create: `gateway-boot/src/main/resources/db/migration/V69__add_model_catalog_sync_fields.sql`
- Modify: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/Model.java`
- Modify: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/ModelDo.java`
- Modify: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/ModelJpaRepository.java`
- Modify: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/JpaModelRepository.java`
- Modify: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/ModelRepository.java`
- Test: `gateway-provider/provider-data/src/test/java/com/codingas/gateway/providerdata/model/JpaModelRepositoryTest.java`

**Interfaces:**
- Consumes: 现有 `Model` 实体字段（modelName/displayName/modelFamily/contextWindow/maxInputTokens/maxOutputTokens/knowledgeCutoff/capabilities/modalities/deprecatedAt/scheduledRetiredAt/deprecationMessage）、`ModelDo` 映射、`ModelRepository`（save/findById/findByModelName/findAll/findByIds/count/delete/findByKeyword/findByCapability）。
- Produces:
  - `Model` 新增字段：`String description`、`LocalDate releaseDate`、`LocalDate lastUpdated`、`String license`、`Boolean openWeights`、`List<Map<String,Object>> benchmarks`、`List<Map<String,Object>> weights`、`String source`（默认 `"MANUAL"`）、`String externalId`、`List<String> lockedFields`。
  - `ModelRepository` 新增方法：`Optional<Model> findByExternalId(String externalId)`。
  - `ModelJpaRepository` 新增方法：`Optional<ModelDo> findByExternalId(String externalId)`。

- [ ] **Step 1: 写 V69 迁移 SQL**

创建 `gateway-boot/src/main/resources/db/migration/V69__add_model_catalog_sync_fields.sql`，顶部加与 V68 一致的版权注释与标题注释（`-- ============================================` 风格）：

```sql
-- models 表新增模型目录同步字段
ALTER TABLE models ADD COLUMN IF NOT EXISTS description TEXT;
ALTER TABLE models ADD COLUMN IF NOT EXISTS release_date DATE;
ALTER TABLE models ADD COLUMN IF NOT EXISTS last_updated DATE;
ALTER TABLE models ADD COLUMN IF NOT EXISTS license VARCHAR(128);
ALTER TABLE models ADD COLUMN IF NOT EXISTS open_weights BOOLEAN;
ALTER TABLE models ADD COLUMN IF NOT EXISTS benchmarks JSONB;
ALTER TABLE models ADD COLUMN IF NOT EXISTS weights JSONB;
ALTER TABLE models ADD COLUMN IF NOT EXISTS source VARCHAR(32) DEFAULT 'MANUAL';
ALTER TABLE models ADD COLUMN IF NOT EXISTS external_id VARCHAR(256);
ALTER TABLE models ADD COLUMN IF NOT EXISTS locked_fields JSONB;
CREATE INDEX IF NOT EXISTS idx_models_external_id ON models(external_id);

-- 模型目录同步日志表
CREATE TABLE IF NOT EXISTS catalog_sync_logs (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    triggered_by VARCHAR(64),
    result VARCHAR(16) NOT NULL,
    added_count INTEGER DEFAULT 0,
    updated_count INTEGER DEFAULT 0,
    skipped_count INTEGER DEFAULT 0,
    failed_count INTEGER DEFAULT 0,
    message TEXT,
    synced_at TIMESTAMP,
    created_by BIGINT,
    created_at TIMESTAMP,
    updated_by BIGINT,
    updated_at TIMESTAMP
);
```

> 若现有迁移中 models 的 id 列风格不是 `GENERATED BY DEFAULT AS IDENTITY`，则改为与现有建表一致（参照 V1__init_schema.sql 中 models 表 id 定义）。

- [ ] **Step 2: Model 实体新增字段**

在 `Model.java` 的 `knowledgeCutoff` 之后（`deprecatedAt` 之前）插入：

```java
    /** 模型描述（来自数据源） */
    private String description;

    /** 发布日期 */
    private LocalDate releaseDate;

    /** 数据源最后更新日期 */
    private LocalDate lastUpdated;

    /** 许可证（如 MIT） */
    private String license;

    /** 是否开源权重 */
    private Boolean openWeights;

    /** 基准测试分数 [{name, score, metric, source}] */
    private List<Map<String, Object>> benchmarks;

    /** 权重/模型卡片链接 [{label, url}] */
    private List<Map<String, Object>> weights;

    /** 数据来源：MODELS_DEV / BUILTIN / MANUAL */
    private String source;

    /** 数据源外部 ID（如 openai/gpt-4o），同步幂等匹配键 */
    private String externalId;

    /** 人工锁定字段名集合（同步不覆盖） */
    private List<String> lockedFields;
```

并在 import 区加 `import java.time.LocalDate;`。默认值：实体创建时 `source` 默认 `"MANUAL"`（在 `BuiltinDataLoader`/新建逻辑中设置，不在实体字段初始化，保持实体纯净——仅在读取为 null 时视为 MANUAL）。

- [ ] **Step 3: ModelDo 新增字段**

在 `ModelDo.java` 的 `knowledgeCutoff` 列之后、`capabilities` 之前插入（JSON 列风格对齐现有 capabilities/modalities 的 `@JdbcTypeCode(SqlTypes.JSON)`）：

```java
    @Column(name = "description", columnDefinition = "text")
    private String description;

    @Column(name = "release_date")
    private LocalDate releaseDate;

    @Column(name = "last_updated")
    private LocalDate lastUpdated;

    @Column(name = "license", length = 128)
    private String license;

    @Column(name = "open_weights")
    private Boolean openWeights;

    @JdbcTypeCode(SqlTypes.JSON)
    @Column(name = "benchmarks", columnDefinition = "jsonb")
    private List<Map<String, Object>> benchmarks;

    @JdbcTypeCode(SqlTypes.JSON)
    @Column(name = "weights", columnDefinition = "jsonb")
    private List<Map<String, Object>> weights;

    @Column(name = "source", length = 32)
    private String source;

    @Column(name = "external_id", length = 256)
    private String externalId;

    @JdbcTypeCode(SqlTypes.JSON)
    @Column(name = "locked_fields", columnDefinition = "jsonb")
    private List<String> lockedFields;
```

import 区加 `import java.time.LocalDate;`（已有 `java.time.Instant`）。

- [ ] **Step 4: Repository 扩展**

`ModelRepository.java` 在 `findByCapability` 后新增：

```java
    /**
     * 根据数据源外部 ID 查找模型（同步幂等匹配）
     */
    Optional<Model> findByExternalId(String externalId);
```

`ModelJpaRepository.java` 在 `findByCapability` 后新增：

```java
    Optional<ModelDo> findByExternalId(String externalId);
```

- [ ] **Step 5: JpaModelRepository 转换补字段**

`JpaModelRepository.java` 的 `toEntity` 在 `setKnowledgeCutoff` 后补：

```java
        entity.setDescription(doObj.getDescription());
        entity.setReleaseDate(doObj.getReleaseDate());
        entity.setLastUpdated(doObj.getLastUpdated());
        entity.setLicense(doObj.getLicense());
        entity.setOpenWeights(doObj.getOpenWeights());
        entity.setBenchmarks(doObj.getBenchmarks());
        entity.setWeights(doObj.getWeights());
        entity.setSource(doObj.getSource());
        entity.setExternalId(doObj.getExternalId());
        entity.setLockedFields(doObj.getLockedFields());
```

`toDo` 在 `setKnowledgeCutoff` 后补同样字段的双向拷贝（`doObj.setXxx(entity.getXxx())`）。

- [ ] **Step 6: 写失败测试（Repository 往返 + findByExternalId）**

在 `gateway-provider/provider-data/src/test/java/com/codingas/gateway/providerdata/model/JpaModelRepositoryTest.java` 追加（先看该文件现有测试风格与注解，对齐 `@DataJpaTest` 或现有用法）：

```java
    @Test
    @DisplayName("保存模型含新增目录字段并可按 externalId 查询")
    void save_modelWithCatalogFields_roundTripAndFindByExternalId() {
        Model model = new Model();
        model.setModelName("gpt-4o");
        model.setDisplayName("GPT-4o");
        model.setExternalId("openai/gpt-4o");
        model.setSource("MODELS_DEV");
        model.setDescription("多模态旗舰模型");
        model.setReleaseDate(LocalDate.of(2024, 5, 13));
        model.setOpenWeights(false);
        model.setLicense("Proprietary");
        model.setBenchmarks(List.of(Map.of("name", "MMMU", "score", 69.1)));
        model.setLockedFields(List.of("displayName"));

        Model saved = repository.save(model);

        assertThat(saved.getId()).isNotNull();
        Optional<Model> found = repository.findByExternalId("openai/gpt-4o");
        assertThat(found).isPresent();
        assertThat(found.get().getDescription()).isEqualTo("多模态旗舰模型");
        assertThat(found.get().getReleaseDate()).isEqualTo(LocalDate.of(2024, 5, 13));
        assertThat(found.get().getBenchmarks()).containsExactly(Map.of("name", "MMMU", "score", 69.1));
        assertThat(found.get().getLockedFields()).containsExactly("displayName");
        assertThat(found.get().getSource()).isEqualTo("MODELS_DEV");
    }
```

- [ ] **Step 7: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider-data -Dtest=JpaModelRepositoryTest`
Expected: 编译失败（Model/ModelDo 无 getDescription 等方法）或测试失败。

- [ ] **Step 8: 实现后重跑测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider-data -Dtest=JpaModelRepositoryTest`
Expected: PASS。同时跑 `./mvnw test -pl gateway-provider/provider` 确认 ModelServiceTest 等既有测试不受影响。

- [ ] **Step 9: 提交**

```bash
git add gateway-boot/src/main/resources/db/migration/V69__add_model_catalog_sync_fields.sql \
        gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/Model.java \
        gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/ModelDo.java \
        gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/ModelJpaRepository.java \
        gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/model/JpaModelRepository.java \
        gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/ModelRepository.java \
        gateway-provider/provider-data/src/test/java/com/codingas/gateway/providerdata/model/JpaModelRepositoryTest.java
git commit -m "feat(model): models 表新增目录同步字段 + catalog_sync_logs 表 + findByExternalId
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 人工编辑保护（locked_fields 采集）

**Files:**
- Modify: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/ModelServiceImpl.java`
- Test: `gateway-provider/provider/src/test/java/com/codingas/gateway/provider/model/ModelServiceTest.java`

**Interfaces:**
- Consumes: Task 1 的 `Model.lockedFields`（`List<String>`）。
- Produces: `ModelServiceImpl.update` 行为变更——把本次更新的字段名追加进 `existing.getLockedFields()`；供 Task 6 同步服务跳过锁定字段。

- [ ] **Step 1: 写失败测试**

在 `ModelServiceTest.java` 的 `UpdateTests` 内追加：

```java
        @Test
        @DisplayName("更新字段时自动加入人工锁定集合")
        void update_editsField_addsToLockedFields() {
            // given
            when(modelRepository.findById(1L)).thenReturn(Optional.of(testModel));
            when(modelRepository.save(any(Model.class))).thenAnswer(inv -> inv.getArgument(0));

            Model request = modelEntity(null, "GPT-4 Renamed", 200000, null);

            // when
            modelService.update(1L, request);

            // then
            ArgumentCaptor<Model> captor = ArgumentCaptor.forClass(Model.class);
            verify(modelRepository).save(captor.capture());
            assertThat(captor.getValue().getLockedFields())
                    .contains("displayName", "contextWindow");
        }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelServiceTest`
Expected: FAIL（lockedFields 为 null/空）。

- [ ] **Step 3: 实现 locked_fields 采集**

`ModelServiceImpl.update` 重构：先收集变更字段集合，更新实体后合并锁定字段。替换 update 方法体：

```java
    @Override
    @Transactional
    public Model update(Long id, Model model) {
        Model existing = modelRepository.findById(id)
            .orElseThrow(() -> new ResourceNotFoundException("Model", id));

        // 记录本次人工修改的字段，加入锁定集合（同步时跳过这些字段，避免覆盖人工编辑）
        List<String> changedFields = new ArrayList<>();
        if (model.getModelName() != null) {
            existing.setModelName(model.getModelName());
            changedFields.add("modelName");
        }
        if (model.getDisplayName() != null) {
            existing.setDisplayName(model.getDisplayName());
            changedFields.add("displayName");
        }
        if (model.getModelFamily() != null) {
            existing.setModelFamily(model.getModelFamily());
            changedFields.add("modelFamily");
        }
        if (model.getContextWindow() != null) {
            existing.setContextWindow(model.getContextWindow());
            changedFields.add("contextWindow");
        }
        if (model.getMaxInputTokens() != null) {
            existing.setMaxInputTokens(model.getMaxInputTokens());
            changedFields.add("maxInputTokens");
        }
        if (model.getMaxOutputTokens() != null) {
            existing.setMaxOutputTokens(model.getMaxOutputTokens());
            changedFields.add("maxOutputTokens");
        }
        if (model.getCapabilities() != null) {
            existing.setCapabilities(model.getCapabilities());
            changedFields.add("capabilities");
        }
        if (model.getModalities() != null) {
            existing.setModalities(model.getModalities());
            changedFields.add("modalities");
        }
        if (model.getKnowledgeCutoff() != null) {
            existing.setKnowledgeCutoff(model.getKnowledgeCutoff());
            changedFields.add("knowledgeCutoff");
        }

        if (!changedFields.isEmpty()) {
            Set<String> locked = new HashSet<>(existing.getLockedFields() == null
                    ? List.of() : existing.getLockedFields());
            locked.addAll(changedFields);
            existing.setLockedFields(new ArrayList<>(locked));
        }

        Model saved = modelRepository.save(existing);
        log.info("模型更新成功, id={}, modelName={}", id, saved.getModelName());
        return saved;
    }
```

> 注意：update 现在同时支持 knowledgeCutoff 更新（原实现遗漏），行为兼容（原实现不更新该字段；本次一并纳入编辑与锁定）。

import 区补充：`java.util.ArrayList`、`java.util.HashSet`、`java.util.Set`（若未引入）。

- [ ] **Step 4: 运行测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelServiceTest`
Expected: PASS（含新增用例与既有 UpdateTests）。

- [ ] **Step 5: 提交**

```bash
git add gateway-provider/provider/src/main/java/com/codingas/gateway/provider/model/ModelServiceImpl.java \
        gateway-provider/provider/src/test/java/com/codingas/gateway/provider/model/ModelServiceTest.java
git commit -m "feat(model): 人工编辑字段自动加入锁定集合，同步不覆盖人工修改
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 同步配置属性 + ModelCatalogClient

**Files:**
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncProperties.java`
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogDto.java`
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogClient.java`
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncException.java`
- Test: `gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogClientTest.java`
- Test resource: `gateway-provider/provider/src/test/resources/modelsdev-sample.json`

**Interfaces:**
- Consumes: Spring `RestClient` 不可用（provider 模块无 spring-web 依赖）→ 使用 JDK `java.net.http.HttpClient`。
- Produces:
  - `CatalogSyncProperties`（`@ConfigurationProperties(prefix = "gateway.catalog.sync")`）：`String url = "https://models.dev/models.json"`、`Duration timeout = Duration.ofSeconds(30)`、`boolean enabled = true`。
  - `ModelCatalogDto` record：`String id, String name, String description, String family, Boolean attachment, Boolean reasoning, Boolean toolCall, Boolean structuredOutput, Boolean temperature, String knowledge, LocalDate releaseDate, LocalDate lastUpdated, Boolean openWeights, ModalitiesDto modalities, LimitDto limit, String license, List<Map<String,Object>> benchmarks, List<Map<String,Object>> weights`。嵌套 record：`ModalitiesDto(List<String> input, List<String> output)`、`LimitDto(Long context, Long output, Long input)`。（JSON 字段用 `@JsonProperty` 映射 camelCase。）
  - `ModelCatalogClient`：`List<ModelCatalogDto> fetch()`——GET `url`，解析 `Map<String, ModelCatalogDto>`（顶层 key 为 canonical ID）后取 values；超时/IO 异常抛 `CatalogSyncException`。
  - `CatalogSyncException extends RuntimeException`。

- [ ] **Step 1: 写失败测试 + 样例 JSON**

创建测试资源 `gateway-provider/provider/src/test/resources/modelsdev-sample.json`（3 个模型，覆盖完整/缺省字段两种形态）：

```json
{
  "openai/gpt-4o": {
    "id": "openai/gpt-4o",
    "name": "GPT-4o",
    "description": "Omnilingual, multimodal flagship model",
    "family": "gpt",
    "attachment": true,
    "reasoning": false,
    "tool_call": true,
    "structured_output": true,
    "temperature": true,
    "knowledge": "2023-10",
    "release_date": "2024-05-13",
    "last_updated": "2026-08-01",
    "modalities": { "input": ["text", "image"], "output": ["text"] },
    "open_weights": false,
    "limit": { "context": 128000, "output": 16384, "input": 128000 },
    "license": "Proprietary",
    "benchmarks": [ { "name": "MMMU", "score": 69.1, "metric": "val" } ],
    "weights": [ { "label": "Model Card", "url": "https://openai.com/index/hello-gpt-4o/" } ]
  },
  "deepseek/deepseek-v4-flash": {
    "id": "deepseek/deepseek-v4-flash",
    "name": "DeepSeek V4 Flash",
    "description": "Fast DeepSeek V4 lane",
    "family": "deepseek-flash",
    "attachment": false,
    "reasoning": true,
    "tool_call": true,
    "structured_output": false,
    "temperature": true,
    "knowledge": "2025-12",
    "release_date": "2026-04-24",
    "last_updated": "2026-08-10",
    "modalities": { "input": ["text"], "output": ["text"] },
    "open_weights": true,
    "limit": { "context": 1000000, "output": 128000, "input": 1000000 }
  },
  "minimal/minimal-model": {
    "id": "minimal/minimal-model",
    "name": "Minimal",
    "description": "Minimal fields",
    "family": null,
    "attachment": false,
    "reasoning": false,
    "tool_call": false,
    "structured_output": null,
    "temperature": true,
    "release_date": "2026-01-01",
    "last_updated": "2026-01-01",
    "modalities": { "input": ["text"], "output": ["text"] },
    "open_weights": false,
    "limit": { "context": 8192 }
  }
}
```

写测试 `ModelCatalogClientTest.java`：用 JDK `com.sun.net.httpserver.HttpServer` 起本地服务返回该 JSON，断言 `fetch()` 返回 3 条、字段解析正确、`minimal` 的 `limit.input/output` 为 null 不抛异常：

```java
@ExtendWith(MockitoExtension.class)
class ModelCatalogClientTest {

    private HttpServer server;
    private String baseUrl;

    @BeforeEach
    void startServer() throws IOException {
        server = HttpServer.create(new InetSocketAddress(0), 0);
        byte[] body = Files.readAllBytes(Path.of(
                "src/test/resources/modelsdev-sample.json"));
        server.createContext("/models.json", exchange -> {
            exchange.getResponseHeaders().add("Content-Type", "application/json");
            exchange.sendResponseHeaders(200, body.length);
            try (var os = exchange.getResponseBody()) { os.write(body); }
        });
        server.start();
        baseUrl = "http://localhost:" + server.getAddress().getPort() + "/models.json";
    }

    @AfterEach
    void stopServer() { server.stop(0); }

    @Test
    @DisplayName("拉取并解析 models.dev 模型数据")
    void fetch_parsesModels() {
        CatalogSyncProperties props = new CatalogSyncProperties();
        props.setUrl(baseUrl);
        props.setTimeout(Duration.ofSeconds(5));
        ModelCatalogClient client = new ModelCatalogClient(HttpClient.newHttpClient(), props);

        List<ModelCatalogDto> models = client.fetch();

        assertThat(models).hasSize(3);
        ModelCatalogDto gpt4o = models.stream()
                .filter(m -> m.id().equals("openai/gpt-4o")).findFirst().orElseThrow();
        assertThat(gpt4o.name()).isEqualTo("GPT-4o");
        assertThat(gpt4o.attachment()).isTrue();
        assertThat(gpt4o.toolCall()).isTrue();
        assertThat(gpt4o.releaseDate()).isEqualTo(LocalDate.of(2024, 5, 13));
        assertThat(gpt4o.limit().context()).isEqualTo(128000L);
        assertThat(gpt4o.modalities().input()).containsExactly("text", "image");
        assertThat(gpt4o.benchmarks()).hasSize(1);

        ModelCatalogDto minimal = models.stream()
                .filter(m -> m.id().equals("minimal/minimal-model")).findFirst().orElseThrow();
        assertThat(minimal.limit().input()).isNull();
        assertThat(minimal.structuredOutput()).isNull();
    }

    @Test
    @DisplayName("HTTP 失败时抛出 CatalogSyncException")
    void fetch_serverError_throwsCatalogSyncException() {
        CatalogSyncProperties props = new CatalogSyncProperties();
        props.setUrl(baseUrl.replace("/models.json", "/missing"));
        props.setTimeout(Duration.ofSeconds(5));
        ModelCatalogClient client = new ModelCatalogClient(HttpClient.newHttpClient(), props);

        assertThatThrownBy(client::fetch).isInstanceOf(CatalogSyncException.class);
    }
}
```

> 若测试运行目录不是模块根目录导致相对路径失效，改用 `getClass().getClassLoader().getResourceAsStream("modelsdev-sample.json")` 读取。

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelCatalogClientTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现配置属性、DTO、异常与客户端**

`CatalogSyncProperties.java`（对齐 `ProviderHealthProperties` 模式）：

```java
@Data
@ConfigurationProperties(prefix = "gateway.catalog.sync")
public class CatalogSyncProperties {
    /** models.dev 模型数据源 URL */
    private String url = "https://models.dev/models.json";
    /** 拉取超时 */
    private Duration timeout = Duration.ofSeconds(30);
    /** 是否启用数据源（false 时 fetch 抛异常） */
    private boolean enabled = true;
}
```

`CatalogSyncException.java`：

```java
public class CatalogSyncException extends RuntimeException {
    public CatalogSyncException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

`ModelCatalogDto.java`（record + 嵌套 record + `@JsonProperty`）：

```java
public record ModelCatalogDto(
        String id, String name, String description, String family,
        Boolean attachment, Boolean reasoning,
        @JsonProperty("tool_call") Boolean toolCall,
        @JsonProperty("structured_output") Boolean structuredOutput,
        Boolean temperature, String knowledge,
        @JsonProperty("release_date") LocalDate releaseDate,
        @JsonProperty("last_updated") LocalDate lastUpdated,
        @JsonProperty("open_weights") Boolean openWeights,
        ModalitiesDto modalities, LimitDto limit,
        String license,
        List<Map<String, Object>> benchmarks,
        List<Map<String, Object>> weights
) {
    public record ModalitiesDto(List<String> input, List<String> output) {}
    public record LimitDto(Long context, Long output, Long input) {}
}
```

`ModelCatalogClient.java`：

```java
@Slf4j
@Component
@RequiredArgsConstructor
public class ModelCatalogClient {

    private final HttpClient httpClient;
    private final CatalogSyncProperties properties;
    private final ObjectMapper objectMapper;

    /**
     * 拉取并解析 models.dev 模型主数据
     *
     * @return 模型目录 DTO 列表
     * @throws CatalogSyncException 拉取或解析失败时
     */
    public List<ModelCatalogDto> fetch() {
        if (!properties.isEnabled()) {
            throw new CatalogSyncException("模型目录数据源已禁用", null);
        }
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(properties.getUrl()))
                    .timeout(properties.getTimeout())
                    .header("Accept", "application/json")
                    .GET()
                    .build();
            HttpResponse<String> response = httpClient.send(request, HttpResponse.BodyHandlers.ofString());
            if (response.statusCode() != 200) {
                throw new CatalogSyncException("模型目录拉取失败, HTTP " + response.statusCode(), null);
            }
            Map<String, ModelCatalogDto> catalog =
                    objectMapper.readValue(response.body(), new TypeReference<>() {});
            return new ArrayList<>(catalog.values());
        } catch (CatalogSyncException e) {
            throw e;
        } catch (IOException | InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new CatalogSyncException("模型目录拉取失败: " + e.getMessage(), e);
        }
    }
}
```

> 注意：`ModelCatalogClient` 构造器同时注入 `HttpClient` 与 `CatalogSyncProperties`（`@RequiredArgsConstructor` 自动生成）。测试直接 `new` 注入。ObjectMapper 由 Spring 容器提供。
> `@ConfigurationPropertiesScan` 已在 `ProviderConfiguration` 开启，`CatalogSyncProperties` 会被自动注册。

- [ ] **Step 4: 运行测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelCatalogClientTest`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ \
        gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ \
        gateway-provider/provider/src/test/resources/modelsdev-sample.json
git commit -m "feat(sync): 新增 models.dev 模型目录客户端（JDK HttpClient + 配置属性）
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: models.dev → Model 映射器

**Files:**
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelsDevModelMapper.java`
- Test: `gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ModelsDevModelMapperTest.java`

**Interfaces:**
- Consumes: Task 1 的 `Model` 新字段、Task 3 的 `ModelCatalogDto`。
- Produces: `ModelsDevModelMapper` 静态方法：
  - `String baseModelName(String externalId)`——canonical ID 末段（`openai/gpt-4o` → `gpt-4o`；无 `/` 原样返回）。
  - `Model toNewModel(ModelCatalogDto dto)`——新建 Model（含能力/限额/元信息映射，`source=MODELS_DEV`、`externalId=dto.id()`、`lockedFields` 空、`modelName=baseModelName`）。
  - `void merge(ModelCatalogDto dto, Model existing)`——更新已有模型，**跳过 `existing.getLockedFields()` 中的字段**。

- [ ] **Step 1: 写失败测试**

`ModelsDevModelMapperTest.java`：

```java
class ModelsDevModelMapperTest {

    @Test
    @DisplayName("canonical ID 末段提取 modelName")
    void baseModelName_extractsLastSegment() {
        assertThat(ModelsDevModelMapper.baseModelName("openai/gpt-4o")).isEqualTo("gpt-4o");
        assertThat(ModelsDevModelMapper.baseModelName("deepseek-v4-flash")).isEqualTo("deepseek-v4-flash");
    }

    @Test
    @DisplayName("新模型完整映射：能力/限额/元信息")
    void toNewModel_mapsAllFields() {
        ModelCatalogDto dto = new ModelCatalogDto(
                "openai/gpt-4o", "GPT-4o", "Omnilingual multimodal", "gpt",
                true, false, true, true, true, "2023-10",
                LocalDate.of(2024, 5, 13), LocalDate.of(2026, 8, 1), false,
                new ModelCatalogDto.ModalitiesDto(List.of("text", "image"), List.of("text")),
                new ModelCatalogDto.LimitDto(128000L, 16384L, 128000L),
                "Proprietary",
                List.of(Map.of("name", "MMMU", "score", 69.1)),
                List.of(Map.of("label", "Model Card", "url", "https://example.com")));

        Model model = ModelsDevModelMapper.toNewModel(dto);

        assertThat(model.getModelName()).isEqualTo("gpt-4o");
        assertThat(model.getDisplayName()).isEqualTo("GPT-4o");
        assertThat(model.getDescription()).isEqualTo("Omnilingual multimodal");
        assertThat(model.getModelFamily()).isEqualTo("gpt");
        assertThat(model.getContextWindow()).isEqualTo(128000);
        assertThat(model.getMaxInputTokens()).isEqualTo(128000);
        assertThat(model.getMaxOutputTokens()).isEqualTo(16384);
        assertThat(model.getKnowledgeCutoff()).isEqualTo("2023-10");
        assertThat(model.getReleaseDate()).isEqualTo(LocalDate.of(2024, 5, 13));
        assertThat(model.getLastUpdated()).isEqualTo(LocalDate.of(2026, 8, 1));
        assertThat(model.getLicense()).isEqualTo("Proprietary");
        assertThat(model.getOpenWeights()).isFalse();
        assertThat(model.getCapabilities())
                .containsEntry("vision", true)
                .containsEntry("reasoning", false)
                .containsEntry("function_calling", true)
                .containsEntry("structured_output", true);
        assertThat(model.getModalities()).containsExactlyInAnyOrder("text", "image");
        assertThat(model.getSource()).isEqualTo("MODELS_DEV");
        assertThat(model.getExternalId()).isEqualTo("openai/gpt-4o");
        assertThat(model.getBenchmarks()).hasSize(1);
        assertThat(model.getWeights()).hasSize(1);
        assertThat(model.getLockedFields()).isNullOrEmpty();
        assertThat(model.isAvailable()).isTrue();
    }

    @Test
    @DisplayName("limit.input 缺失时回退 contextWindow")
    void toNewModel_limitInputMissing_fallsBackToContext() {
        ModelCatalogDto dto = new ModelCatalogDto(
                "minimal/minimal-model", "Minimal", "desc", null,
                false, false, false, null, true, null,
                LocalDate.of(2026, 1, 1), LocalDate.of(2026, 1, 1), false,
                new ModelCatalogDto.ModalitiesDto(List.of("text"), List.of("text")),
                new ModelCatalogDto.LimitDto(8192L, null, null), null, null, null);

        Model model = ModelsDevModelMapper.toNewModel(dto);

        assertThat(model.getContextWindow()).isEqualTo(8192);
        assertThat(model.getMaxInputTokens()).isEqualTo(8192);
        assertThat(model.getMaxOutputTokens()).isNull();
        assertThat(model.getCapabilities()).containsEntry("vision", false);
        assertThat(model.getModalities()).containsExactly("text");
    }

    @Test
    @DisplayName("merge 跳过人工锁定字段")
    void merge_skipsLockedFields() {
        ModelCatalogDto dto = new ModelCatalogDto(
                "openai/gpt-4o", "GPT-4o new", "new desc", "gpt-new",
                true, true, true, true, true, "2024-01",
                LocalDate.of(2025, 1, 1), LocalDate.of(2026, 8, 1), false,
                new ModelCatalogDto.ModalitiesDto(List.of("text"), List.of("text")),
                new ModelCatalogDto.LimitDto(200000L, 100000L, 200000L),
                "MIT", List.of(), List.of());

        Model existing = new Model();
        existing.setModelName("gpt-4o");
        existing.setDisplayName("GPT-4o old");
        existing.setContextWindow(128000);
        existing.setLockedFields(List.of("displayName", "contextWindow"));

        ModelsDevModelMapper.merge(dto, existing);

        // 锁定字段不被覆盖
        assertThat(existing.getDisplayName()).isEqualTo("GPT-4o old");
        assertThat(existing.getContextWindow()).isEqualTo(128000);
        // 未锁定字段被更新
        assertThat(existing.getDescription()).isEqualTo("new desc");
        assertThat(existing.getModelFamily()).isEqualTo("gpt-new");
        assertThat(existing.getMaxInputTokens()).isEqualTo(200000);
        assertThat(existing.getMaxOutputTokens()).isEqualTo(100000);
        assertThat(existing.getKnowledgeCutoff()).isEqualTo("2024-01");
        assertThat(existing.getReleaseDate()).isEqualTo(LocalDate.of(2025, 1, 1));
        assertThat(existing.getLicense()).isEqualTo("MIT");
        assertThat(existing.getCapabilities()).containsEntry("reasoning", true);
        // externalId/source 由同步服务负责，merge 不修改 modelName
        assertThat(existing.getModelName()).isEqualTo("gpt-4o");
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelsDevModelMapperTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现映射器**

`ModelsDevModelMapper.java`（纯静态方法，无 Spring 依赖）：

```java
/**
 * models.dev 模型目录 → Model 实体映射器
 *
 * <p>纯静态工具：canonical ID 提取 modelName、能力/限额/元信息字段映射、
 * 人工锁定字段保护（merge 跳过 lockedFields）。</p>
 */
public final class ModelsDevModelMapper {

    private ModelsDevModelMapper() {}

    /** models.dev 数据源标识 */
    public static final String SOURCE = "MODELS_DEV";

    /**
     * 提取 canonical ID 末段作为通用模型名
     */
    public static String baseModelName(String externalId) {
        int idx = externalId.lastIndexOf('/');
        return idx >= 0 ? externalId.substring(idx + 1) : externalId;
    }

    /**
     * 由目录 DTO 新建 Model 实体
     */
    public static Model toNewModel(ModelCatalogDto dto) {
        Model model = new Model();
        model.setModelName(baseModelName(dto.id()));
        model.setDisplayName(dto.name());
        model.setDescription(dto.description());
        model.setModelFamily(dto.family());
        model.setSource(SOURCE);
        model.setExternalId(dto.id());
        model.setReleaseDate(dto.releaseDate());
        model.setLastUpdated(dto.lastUpdated());
        model.setLicense(dto.license());
        model.setOpenWeights(dto.openWeights());
        model.setBenchmarks(dto.benchmarks());
        model.setWeights(dto.weights());
        applyLimits(dto, model);
        model.setKnowledgeCutoff(dto.knowledge());
        model.setCapabilities(toCapabilities(dto));
        model.setModalities(toModalities(dto));
        return model;
    }

    /**
     * 合并目录 DTO 到已有模型，跳过人工锁定字段
     */
    public static void merge(ModelCatalogDto dto, Model existing) {
        List<String> locked = existing.getLockedFields() == null
                ? List.of() : existing.getLockedFields();
        if (!locked.contains("displayName")) existing.setDisplayName(dto.name());
        if (!locked.contains("description")) existing.setDescription(dto.description());
        if (!locked.contains("modelFamily")) existing.setModelFamily(dto.family());
        if (!locked.contains("knowledgeCutoff")) existing.setKnowledgeCutoff(dto.knowledge());
        if (!locked.contains("contextWindow")) {
            existing.setContextWindow(intValue(dto.limit() == null ? null : dto.limit().context()));
        }
        if (!locked.contains("maxInputTokens")) {
            existing.setMaxInputTokens(resolveInputLimit(dto));
        }
        if (!locked.contains("maxOutputTokens")) {
            existing.setMaxOutputTokens(intValue(dto.limit() == null ? null : dto.limit().output()));
        }
        if (!locked.contains("capabilities")) existing.setCapabilities(toCapabilities(dto));
        if (!locked.contains("modalities")) existing.setModalities(toModalities(dto));
        if (!locked.contains("releaseDate")) existing.setReleaseDate(dto.releaseDate());
        if (!locked.contains("lastUpdated")) existing.setLastUpdated(dto.lastUpdated());
        if (!locked.contains("license")) existing.setLicense(dto.license());
        if (!locked.contains("openWeights")) existing.setOpenWeights(dto.openWeights());
        if (!locked.contains("benchmarks")) existing.setBenchmarks(dto.benchmarks());
        if (!locked.contains("weights")) existing.setWeights(dto.weights());
    }

    private static void applyLimits(ModelCatalogDto dto, Model model) {
        if (dto.limit() == null) return;
        model.setContextWindow(intValue(dto.limit().context()));
        model.setMaxInputTokens(resolveInputLimit(dto));
        model.setMaxOutputTokens(intValue(dto.limit().output()));
    }

    /** limit.input 缺失时回退 context */
    private static Integer resolveInputLimit(ModelCatalogDto dto) {
        if (dto.limit() == null) return null;
        return dto.limit().input() != null ? intValue(dto.limit().input())
                : intValue(dto.limit().context());
    }

    private static Integer intValue(Long value) {
        return value == null ? null : value.intValue();
    }

    /** 能力映射：attachment→vision、reasoning、tool_call→function_calling、structured_output */
    private static Map<String, Boolean> toCapabilities(ModelCatalogDto dto) {
        Map<String, Boolean> caps = new LinkedHashMap<>();
        caps.put("vision", Boolean.TRUE.equals(dto.attachment()));
        caps.put("reasoning", Boolean.TRUE.equals(dto.reasoning()));
        caps.put("function_calling", Boolean.TRUE.equals(dto.toolCall()));
        caps.put("structured_output", Boolean.TRUE.equals(dto.structuredOutput()));
        return caps;
    }

    /** 模态：输入+输出合并去重 */
    private static List<String> toModalities(ModelCatalogDto dto) {
        if (dto.modalities() == null) return List.of();
        LinkedHashSet<String> merged = new LinkedHashSet<>();
        if (dto.modalities().input() != null) merged.addAll(dto.modalities().input());
        if (dto.modalities().output() != null) merged.addAll(dto.modalities().output());
        return new ArrayList<>(merged);
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelsDevModelMapperTest`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelsDevModelMapper.java \
        gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ModelsDevModelMapperTest.java
git commit -m "feat(sync): models.dev 目录→Model 映射器（能力/限额/元信息映射 + 锁定字段保护）
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 同步日志实体与 Repository

**Files:**
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncLog.java`
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncLogRepository.java`
- Create: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/catalog/sync/CatalogSyncLogDo.java`
- Create: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/catalog/sync/CatalogSyncLogJpaRepository.java`
- Create: `gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/catalog/sync/JpaCatalogSyncLogRepository.java`
- Test: `gateway-provider/provider-data/src/test/java/com/codingas/gateway/providerdata/catalog/sync/JpaCatalogSyncLogRepositoryTest.java`

**Interfaces:**
- Consumes: Task 1 的 `catalog_sync_logs` 表。
- Produces:
  - `CatalogSyncLog` 实体（继承 `BaseEntity`）：`String triggeredBy`、`String result`（`SUCCESS`/`FAILURE`）、`int addedCount/updatedCount/skippedCount/failedCount`、`String message`、`Instant syncedAt`。
  - `CatalogSyncLogRepository`：`CatalogSyncLog save(CatalogSyncLog log)`、`Optional<CatalogSyncLog> findLatest()`。
  - `CatalogSyncLogDo` / `CatalogSyncLogJpaRepository` / `JpaCatalogSyncLogRepository` 实现。

- [ ] **Step 1: 写失败测试**

`JpaCatalogSyncLogRepositoryTest.java`（对齐 JpaModelRepositoryTest 风格，用 `@DataJpaTest` + `@Import` 或直接注入 `JpaCatalogSyncLogRepository`）：

```java
@DataJpaTest
class JpaCatalogSyncLogRepositoryTest {

    @Autowired
    private CatalogSyncLogJpaRepository jpaRepository;

    private JpaCatalogSyncLogRepository repository;

    @BeforeEach
    void setUp() {
        repository = new JpaCatalogSyncLogRepository(jpaRepository);
    }

    @Test
    @DisplayName("保存日志并查询最近一条")
    void saveAndFindLatest_returnsNewest() {
        CatalogSyncLog first = log("SUCCESS", 100, 20, 0, 0, "首次全量");
        CatalogSyncLog second = log("SUCCESS", 0, 5, 1, 0, "二次增量");
        repository.save(first);
        repository.save(second);

        Optional<CatalogSyncLog> latest = repository.findLatest();

        assertThat(latest).isPresent();
        assertThat(latest.get().getMessage()).isEqualTo("二次增量");
        assertThat(latest.get().getAddedCount()).isEqualTo(0);
    }

    private CatalogSyncLog log(String result, int added, int updated, int skipped, int failed, String message) {
        CatalogSyncLog log = new CatalogSyncLog();
        log.setResult(result);
        log.setAddedCount(added);
        log.setUpdatedCount(updated);
        log.setSkippedCount(skipped);
        log.setFailedCount(failed);
        log.setMessage(message);
        log.setSyncedAt(Instant.now());
        return log;
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider-data -Dtest=JpaCatalogSyncLogRepositoryTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现实体、Repository 与 JPA 层**

`CatalogSyncLog.java`（继承 `BaseEntity`，字段如 Interfaces 定义，中文 Javadoc 说明每个字段）。

`CatalogSyncLogDo.java`（`@Entity @Table(name = "catalog_sync_logs")`，映射 `triggered_by/result/added_count/updated_count/skipped_count/failed_count/message/synced_at`；继承 `BaseDo`）。

`CatalogSyncLogJpaRepository.java`：

```java
public interface CatalogSyncLogJpaRepository extends JpaRepository<CatalogSyncLogDo, Long> {
    Optional<CatalogSyncLogDo> findTopByOrderBySyncedAtDesc();
}
```

`CatalogSyncLogRepository.java`（domain 接口）：

```java
public interface CatalogSyncLogRepository {
    CatalogSyncLog save(CatalogSyncLog log);
    Optional<CatalogSyncLog> findLatest();
}
```

`JpaCatalogSyncLogRepository.java`（`@Component`，toEntity/toDo 双向转换，`findLatest` 委托 `findTopByOrderBySyncedAtDesc`）。

- [ ] **Step 4: 运行测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider-data -Dtest=JpaCatalogSyncLogRepositoryTest`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncLog.java \
        gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncLogRepository.java \
        gateway-provider/provider-data/src/main/java/com/codingas/gateway/providerdata/catalog/sync/ \
        gateway-provider/provider-data/src/test/java/com/codingas/gateway/providerdata/catalog/sync/
git commit -m "feat(sync): 目录同步日志实体与 Repository（catalog_sync_logs）
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: ModelCatalogSyncService（同步编排）

**Files:**
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogSyncService.java`
- Create: `gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncReport.java`
- Test: `gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogSyncServiceTest.java`

**Interfaces:**
- Consumes: Task 3 `ModelCatalogClient.fetch()`、Task 4 `ModelsDevModelMapper`、Task 5 `CatalogSyncLogRepository`、Task 1 `ModelRepository`（`findByExternalId`/`findByModelName`/`save`/`findAll`）。
- Produces:
  - `CatalogSyncReport`（`@Data` 类或 record）：`boolean success`、`int addedCount/updatedCount/skippedCount/failedCount`、`List<String> messages`、`Instant syncedAt`。
  - `ModelCatalogSyncService.sync()`：返回 `CatalogSyncReport`；拉取失败时记录 `FAILURE` 日志并抛 `CatalogSyncException`。

- [ ] **Step 1: 写失败测试**

`ModelCatalogSyncServiceTest.java`（Mockito mock client/repository/logRepository）：

```java
@ExtendWith(MockitoExtension.class)
class ModelCatalogSyncServiceTest {

    @Mock private ModelCatalogClient client;
    @Mock private ModelRepository modelRepository;
    @Mock private CatalogSyncLogRepository logRepository;

    private ModelCatalogSyncService service;

    private final ModelCatalogDto gpt4o = new ModelCatalogDto(
            "openai/gpt-4o", "GPT-4o", "desc", "gpt",
            true, false, true, true, true, "2023-10",
            LocalDate.of(2024, 5, 13), LocalDate.of(2026, 8, 1), false,
            new ModelCatalogDto.ModalitiesDto(List.of("text", "image"), List.of("text")),
            new ModelCatalogDto.LimitDto(128000L, 16384L, 128000L),
            "Proprietary", List.of(), List.of());

    private final ModelCatalogDto deepseek = new ModelCatalogDto(
            "deepseek/deepseek-v4-flash", "DeepSeek V4 Flash", "desc2", "deepseek-flash",
            false, true, true, false, true, "2025-12",
            LocalDate.of(2026, 4, 24), LocalDate.of(2026, 8, 10), true,
            new ModelCatalogDto.ModalitiesDto(List.of("text"), List.of("text")),
            new ModelCatalogDto.LimitDto(1000000L, 128000L, 1000000L),
            "MIT", List.of(), List.of());

    @BeforeEach
    void setUp() {
        service = new ModelCatalogSyncService(client, modelRepository, logRepository);
    }

    @Test
    @DisplayName("全量同步：新增+更新+跳过统计正确")
    void sync_mixed_addsUpdatesSkips() {
        // 已有模型：gpt-4o（externalId 匹配，将更新）
        Model existingGpt = new Model();
        existingGpt.setId(1L);
        existingGpt.setModelName("gpt-4o");
        existingGpt.setExternalId("openai/gpt-4o");
        existingGpt.setLockedFields(List.of("displayName"));

        when(client.fetch()).thenReturn(List.of(gpt4o, deepseek));
        when(modelRepository.findByExternalId("openai/gpt-4o")).thenReturn(Optional.of(existingGpt));
        when(modelRepository.findByExternalId("deepseek/deepseek-v4-flash")).thenReturn(Optional.empty());
        when(modelRepository.findByModelName("deepseek-v4-flash")).thenReturn(Optional.empty());
        when(modelRepository.save(any(Model.class))).thenAnswer(inv -> inv.getArgument(0));
        when(logRepository.save(any(CatalogSyncLog.class))).thenAnswer(inv -> inv.getArgument(0));

        CatalogSyncReport report = service.sync();

        assertThat(report.isSuccess()).isTrue();
        assertThat(report.getAddedCount()).isEqualTo(1);
        assertThat(report.getUpdatedCount()).isEqualTo(1);
        assertThat(report.getSkippedCount()).isZero();
        assertThat(report.getFailedCount()).isZero();
        // 新增模型写入 externalId
        ArgumentCaptor<Model> savedCaptor = ArgumentCaptor.forClass(Model.class);
        verify(modelRepository, times(2)).save(savedCaptor.capture());
        Model added = savedCaptor.getAllValues().stream()
                .filter(m -> m.getExternalId().equals("deepseek/deepseek-v4-flash")).findFirst().orElseThrow();
        assertThat(added.getSource()).isEqualTo("MODELS_DEV");
        assertThat(added.getModelName()).isEqualTo("deepseek-v4-flash");
    }

    @Test
    @DisplayName("modelName 冲突：跳过并计入报告")
    void sync_modelNameConflict_skips() {
        // 已有一个 external_id 不同但 modelName 相同的模型
        Model conflict = new Model();
        conflict.setId(2L);
        conflict.setModelName("gpt-4o");
        conflict.setExternalId("other/gpt-4o");

        when(client.fetch()).thenReturn(List.of(gpt4o));
        when(modelRepository.findByExternalId("openai/gpt-4o")).thenReturn(Optional.empty());
        when(modelRepository.findByModelName("gpt-4o")).thenReturn(Optional.of(conflict));
        when(logRepository.save(any(CatalogSyncLog.class))).thenAnswer(inv -> inv.getArgument(0));

        CatalogSyncReport report = service.sync();

        assertThat(report.getSkippedCount()).isEqualTo(1);
        assertThat(report.getAddedCount()).isZero();
        verify(modelRepository, never()).save(any(Model.class));
    }

    @Test
    @DisplayName("拉取失败：记录 FAILURE 日志并抛异常")
    void sync_fetchFailure_logsFailureAndThrows() {
        when(client.fetch()).thenThrow(new CatalogSyncException("网络错误", null));
        when(logRepository.save(any(CatalogSyncLog.class))).thenAnswer(inv -> inv.getArgument(0));

        assertThatThrownBy(() -> service.sync()).isInstanceOf(CatalogSyncException.class);

        ArgumentCaptor<CatalogSyncLog> captor = ArgumentCaptor.forClass(CatalogSyncLog.class);
        verify(logRepository).save(captor.capture());
        assertThat(captor.getValue().getResult()).isEqualTo("FAILURE");
    }

    @Test
    @DisplayName("存量模型无 externalId：按 modelName 匹配并补写 externalId")
    void sync_existingModelWithoutExternalId_backfillsExternalId() {
        Model legacy = new Model();
        legacy.setId(3L);
        legacy.setModelName("gpt-4o");
        legacy.setSource("BUILTIN");

        when(client.fetch()).thenReturn(List.of(gpt4o));
        when(modelRepository.findByExternalId("openai/gpt-4o")).thenReturn(Optional.empty());
        when(modelRepository.findByModelName("gpt-4o")).thenReturn(Optional.of(legacy));
        when(modelRepository.save(any(Model.class))).thenAnswer(inv -> inv.getArgument(0));
        when(logRepository.save(any(CatalogSyncLog.class))).thenAnswer(inv -> inv.getArgument(0));

        CatalogSyncReport report = service.sync();

        assertThat(report.getUpdatedCount()).isEqualTo(1);
        assertThat(legacy.getExternalId()).isEqualTo("openai/gpt-4o");
        assertThat(legacy.getSource()).isEqualTo("BUILTIN");  // 不改变来源标记
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelCatalogSyncServiceTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现报告与服务**

`CatalogSyncReport.java`：

```java
@Data
@Builder
public class CatalogSyncReport {
    private boolean success;
    private int addedCount;
    private int updatedCount;
    private int skippedCount;
    private int failedCount;
    private List<String> messages;
    private Instant syncedAt;
}
```

`ModelCatalogSyncService.java`：

```java
@Slf4j
@Service
@RequiredArgsConstructor
public class ModelCatalogSyncService {

    private final ModelCatalogClient client;
    private final ModelRepository modelRepository;
    private final CatalogSyncLogRepository logRepository;

    /**
     * 执行模型目录同步
     *
     * <p>拉取 models.dev 模型主数据，按 external_id（次选 model_name）幂等 upsert：
     * 新增模型写入完整信息；已有模型由 {@link ModelsDevModelMapper#merge} 更新并跳过
     * 人工锁定字段；modelName 冲突的模型跳过并计入报告。不删除 models.dev 未出现的
     * 现有模型。结果写入 catalog_sync_logs。</p>
     *
     * @return 同步报告
     * @throws CatalogSyncException 拉取失败（已记录 FAILURE 日志）
     */
    @Transactional
    public CatalogSyncReport sync() {
        Instant startedAt = Instant.now();
        CatalogSyncReport.CatalogSyncReportBuilder report = CatalogSyncReport.builder()
                .success(true).syncedAt(startedAt).messages(new ArrayList<>());
        try {
            List<ModelCatalogDto> models = client.fetch();
            for (ModelCatalogDto dto : models) {
                upsertModel(dto, report);
            }
            saveLog(report, null, startedAt);
            log.info("模型目录同步完成: added={}, updated={}, skipped={}",
                    report.build().getAddedCount(), report.build().getUpdatedCount(),
                    report.build().getSkippedCount());
            return report.build();
        } catch (CatalogSyncException e) {
            saveLog(report, e.getMessage(), startedAt);
            log.error("模型目录同步失败: {}", e.getMessage(), e);
            throw e;
        }
    }

    private void upsertModel(ModelCatalogDto dto, CatalogSyncReport.CatalogSyncReportBuilder report) {
        Optional<Model> byExternal = modelRepository.findByExternalId(dto.id());
        Optional<Model> byName = byExternal.isPresent() ? Optional.empty()
                : modelRepository.findByModelName(ModelsDevModelMapper.baseModelName(dto.id()));

        if (byExternal.isPresent()) {
            Model existing = byExternal.get();
            ModelsDevModelMapper.merge(dto, existing);
            modelRepository.save(existing);
            report.updatedCount(report.build().getUpdatedCount() + 1);
        } else if (byName.isPresent()) {
            Model existing = byName.get();
            if (existing.getExternalId() != null && !existing.getExternalId().equals(dto.id())) {
                // modelName 已被其他模型占用：冲突跳过
                report.skippedCount(report.build().getSkippedCount() + 1);
                report.messages(List.of(...));  // 追加冲突明细
            } else {
                existing.setExternalId(dto.id());  // 存量模型补写 externalId
                ModelsDevModelMapper.merge(dto, existing);
                modelRepository.save(existing);
                report.updatedCount(report.build().getUpdatedCount() + 1);
            }
        } else {
            modelRepository.save(ModelsDevModelMapper.toNewModel(dto));
            report.addedCount(report.build().getAddedCount() + 1);
        }
    }

    private void saveLog(CatalogSyncReport.CatalogSyncReportBuilder report, String error, Instant syncedAt) {
        CatalogSyncLog log = new CatalogSyncLog();
        CatalogSyncReport built = report.build();
        log.setResult(error == null ? "SUCCESS" : "FAILURE");
        log.setAddedCount(built.getAddedCount());
        log.setUpdatedCount(built.getUpdatedCount());
        log.setSkippedCount(built.getSkippedCount());
        log.setFailedCount(built.getFailedCount());
        log.setMessage(error != null ? error : "同步完成");
        log.setSyncedAt(syncedAt);
        try {
            logRepository.save(log);
        } catch (Exception e) {
            log.warn("保存同步日志失败: {}", e.getMessage());
        }
    }
}
```

> 注意：`CatalogSyncReport` 的 builder 在循环中累加计数。若 `@Builder` 的增量写法繁琐，可将报告改为可变类（`@Data` + 字段 setter 自增方法 `addAdded()` 等），实现类时保持语义即可；测试断言只依赖 getter。**建议**：`CatalogSyncReport` 用 `@Data` + `@Builder`，并提供 `incrementAdded()/incrementUpdated()/incrementSkipped()/addMessage(String)` 四个便捷方法简化累加。冲突消息用 `addMessage` 追加（如 `"modelName 冲突跳过: gpt-4o (externalId=other/gpt-4o)"`）。服务实现相应改用这些方法（比 `report.build()` 中间快照更清晰）。

- [ ] **Step 4: 运行测试确认通过**

Run: `./mvnw test -pl gateway-provider/provider -Dtest=ModelCatalogSyncServiceTest`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogSyncService.java \
        gateway-provider/provider/src/main/java/com/codingas/gateway/provider/catalog/sync/CatalogSyncReport.java \
        gateway-provider/provider/src/test/java/com/codingas/gateway/provider/catalog/sync/ModelCatalogSyncServiceTest.java
git commit -m "feat(sync): 模型目录同步编排服务（幂等 upsert + 冲突跳过 + 日志落库）
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: CatalogSyncController + Web DTO

**Files:**
- Create: `gateway-web/src/main/java/com/codingas/gateway/web/api/CatalogSyncController.java`
- Create: `gateway-web/src/main/java/com/codingas/gateway/web/api/dto/CatalogSyncReportResponse.java`
- Create: `gateway-web/src/main/java/com/codingas/gateway/web/api/dto/CatalogSyncStatusResponse.java`
- Modify: `gateway-web/src/main/java/com/codingas/gateway/web/api/dto/ModelResponse.java`（新增字段透出）
- Test: `gateway-web/src/test/java/com/codingas/gateway/web/api/CatalogSyncControllerTest.java`

**Interfaces:**
- Consumes: Task 6 `ModelCatalogSyncService.sync()`、Task 5 `CatalogSyncLogRepository.findLatest()`、`CatalogSyncReport`。
- Produces:
  - `POST /api/v1/catalog/sync` → `CatalogSyncReportResponse`（success/addedCount/updatedCount/skippedCount/failedCount/messages/syncedAt）。
  - `GET /api/v1/catalog/sync/status` → `CatalogSyncStatusResponse`（result/addedCount/updatedCount/skippedCount/failedCount/message/syncedAt；无记录时返回 204 或空体）。
  - `ModelResponse` 新增字段：`description/releaseDate/lastUpdated/license/openWeights/benchmarks/weights/source/externalId`（透出同步后的模型元信息）。

- [ ] **Step 1: 写失败测试**

`CatalogSyncControllerTest.java`（对齐 `ModelControllerTest` 的 MockMvc + @WebMvcTest 风格；先读该文件确认注解与 mock 方式）：

```java
@WebMvcTest(CatalogSyncController.class)
class CatalogSyncControllerTest {

    @Autowired private MockMvc mockMvc;
    @MockBean private ModelCatalogSyncService syncService;
    @MockBean private CatalogSyncLogRepository logRepository;

    @Test
    @DisplayName("POST /api/v1/catalog/sync 触发同步并返回报告")
    void sync_returnsReport() throws Exception {
        CatalogSyncReport report = CatalogSyncReport.builder()
                .success(true).addedCount(100).updatedCount(20)
                .skippedCount(0).failedCount(0)
                .messages(List.of("同步完成")).syncedAt(Instant.parse("2026-08-28T00:00:00Z"))
                .build();
        when(syncService.sync()).thenReturn(report);

        mockMvc.perform(post("/api/v1/catalog/sync"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.success").value(true))
                .andExpect(jsonPath("$.addedCount").value(100))
                .andExpect(jsonPath("$.updatedCount").value(20));
    }

    @Test
    @DisplayName("GET /api/v1/catalog/sync/status 返回最近同步记录")
    void status_returnsLatestLog() throws Exception {
        CatalogSyncLog log = new CatalogSyncLog();
        log.setResult("SUCCESS");
        log.setAddedCount(100);
        log.setUpdatedCount(20);
        log.setSyncedAt(Instant.parse("2026-08-28T00:00:00Z"));
        when(logRepository.findLatest()).thenReturn(Optional.of(log));

        mockMvc.perform(get("/api/v1/catalog/sync/status"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.result").value("SUCCESS"))
                .andExpect(jsonPath("$.addedCount").value(100));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw test -pl gateway-web -Dtest=CatalogSyncControllerTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现 DTO 与 Controller**

`CatalogSyncReportResponse.java` / `CatalogSyncStatusResponse.java`：`@Data` + 静态 `from(...)` 工厂（对齐 `ModelResponse.from` 模式）。

`CatalogSyncController.java`：

```java
@RestController
@RequestMapping("/api/v1/catalog/sync")
@RequiredArgsConstructor
public class CatalogSyncController {

    private final ModelCatalogSyncService syncService;
    private final CatalogSyncLogRepository logRepository;

    /**
     * 手工触发模型目录同步
     */
    @PostMapping
    public CatalogSyncReportResponse sync() {
        return CatalogSyncReportResponse.from(syncService.sync());
    }

    /**
     * 查询最近一次同步状态
     */
    @GetMapping("/status")
    public ResponseEntity<CatalogSyncStatusResponse> status() {
        return logRepository.findLatest()
                .map(l -> ResponseEntity.ok(CatalogSyncStatusResponse.from(l)))
                .orElseGet(() -> ResponseEntity.noContent().build());
    }
}
```

`ModelResponse.java`：新增字段声明 + `from` 方法中透出 `model.getDescription()/getReleaseDate()/getLastUpdated()/getLicense()/getOpenWeights()/getBenchmarks()/getWeights()/getSource()/getExternalId()`。

- [ ] **Step 4: 运行测试确认通过 + 回归**

Run: `./mvnw test -pl gateway-web -Dtest=CatalogSyncControllerTest`
Expected: PASS。
Run: `./mvnw test -pl gateway-web -Dtest=ModelControllerTest`
Expected: PASS（ModelResponse 变更不影响既有断言）。

- [ ] **Step 5: 提交**

```bash
git add gateway-web/src/main/java/com/codingas/gateway/web/api/CatalogSyncController.java \
        gateway-web/src/main/java/com/codingas/gateway/web/api/dto/CatalogSyncReportResponse.java \
        gateway-web/src/main/java/com/codingas/gateway/web/api/dto/CatalogSyncStatusResponse.java \
        gateway-web/src/main/java/com/codingas/gateway/web/api/dto/ModelResponse.java \
        gateway-web/src/test/java/com/codingas/gateway/web/api/CatalogSyncControllerTest.java
git commit -m "feat(web): 模型目录同步控制器（手工触发 + 状态查询）+ ModelResponse 透出元信息
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 前端（Models 字段展示 + Catalog 同步入口）

**Files:**
- Modify: `gateway-console/src/types/model.ts`
- Modify: `gateway-console/src/pages/Models/index.tsx`
- Modify: `gateway-console/src/pages/Catalog/index.tsx`
- Create: `gateway-console/src/services/api/catalogSync.ts`
- Modify: `gateway-console/src/services/query/useCatalog.ts`（或新建 `useCatalogSync.ts`）
- Modify: `gateway-console/src/types/catalog.ts`

**Interfaces:**
- Consumes: Task 7 的 `POST /api/v1/catalog/sync`、`GET /api/v1/catalog/sync/status`、`ModelResponse` 新字段。
- Produces: 前端类型与交互。

- [ ] **Step 1: 扩展模型类型**

`types/model.ts` 的 `Model` 接口追加：

```ts
  description?: string;
  releaseDate?: string;
  lastUpdated?: string;
  license?: string;
  openWeights?: boolean;
  benchmarks?: Array<Record<string, unknown>>;
  weights?: Array<Record<string, unknown>>;
  source?: string;
  externalId?: string;
```

- [ ] **Step 2: 新增同步 API 与 hook**

`services/api/catalogSync.ts`（对齐 `services/api/catalog.ts` 的 axios/fetch 封装风格）：

```ts
// 手工触发模型目录同步
export function syncModelCatalog() {
  return http.post('/api/v1/catalog/sync');
}
// 查询最近同步状态
export function getCatalogSyncStatus() {
  return http.get('/api/v1/catalog/sync/status');
}
```

`services/query/useCatalog.ts` 新增 `useCatalogSync()`（`useMutation` + 状态查询），或新建 `useCatalogSync.ts`（对齐现有 useXxx 命名与 react-query 用法）。

- [ ] **Step 3: Models 页展示新字段**

`pages/Models/index.tsx`：在模型表格或详情中展示 `license`、`source`、`releaseDate`、`openWeights`（有值才显示，对齐现有列渲染风格）。

- [ ] **Step 4: Catalog 页同步入口**

`pages/Catalog/index.tsx`：新增"模型目录同步"区域——显示最近同步时间/结果（`getCatalogSyncStatus`），"同步模型目录"按钮（`syncModelCatalog`，成功后 `queryClient.invalidateQueries` 刷新模型列表）。

- [ ] **Step 5: 前端测试/构建**

Run: `cd gateway-console && npm run build`（或项目既有的 lint/build 命令）
Expected: 构建通过，无类型错误。

- [ ] **Step 6: 提交**

```bash
git add gateway-console/src/types/model.ts gateway-console/src/types/catalog.ts \
        gateway-console/src/pages/Models/index.tsx gateway-console/src/pages/Catalog/index.tsx \
        gateway-console/src/services/api/catalogSync.ts gateway-console/src/services/query/
git commit -m "feat(console): 模型元信息展示 + 模型目录手工同步入口
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: 全流程集成验证

**Files:**
- Modify: `gateway-boot/src/test/...`（集成测试，若项目有 boot 级集成测试基建则对齐；否则手工验证）
- 无源码改动（验证任务）

**Interfaces:**
- Consumes: 全部前置任务产物。

- [ ] **Step 1: 全量构建与测试**

Run: `./mvnw clean install`
Expected: 全模块 BUILD SUCCESS，测试全绿（含 gateway-coverage 聚合）。

- [ ] **Step 2: 启动应用手工验证（首次全量）**

Run: `./mvnw spring-boot:run -pl gateway-boot`
操作：
1. 调 `GET /api/v1/models`（分页）确认 models 表含 BUILTIN 旧模型 + 手动 `POST /api/v1/catalog/sync` 前为 75+。
2. `POST /api/v1/catalog/sync` → 返回报告（added≈361-75、updated≈匹配数）。
3. `GET /api/v1/catalog/sync/status` → 显示 SUCCESS 与计数。
4. `GET /api/v1/models?page=1&limit=10` → 新模型带 externalId/description/license/source=MODELS_DEV。
5. `GET /api/v1/models?keyword=deepseek` → deepseek-v4-flash 存在。

Expected: 全部符合预期。

- [ ] **Step 3: 二次同步验证幂等**

操作：再次 `POST /api/v1/catalog/sync`。
Expected: added=0、updated≈0（或少量 last_updated 变化），不重复创建。

- [ ] **Step 4: 人工锁定保护验证**

操作：
1. `PUT /api/v1/models/{id}` 修改 displayName（如 deepseek-v4-flash 改名）。
2. 再次 `POST /api/v1/catalog/sync`。
Expected: 该模型 displayName 保持人工修改值（locked_fields 生效），其余字段仍更新。

- [ ] **Step 5: 旧模型保留验证**

操作：确认 BUILTIN 旧模型（如 deepseek-chat）仍在 models 表中，未被删除。
Expected: 保留，`GET /api/v1/models?keyword=deepseek-chat` 有结果。

- [ ] **Step 6: 提交（若验证中发现修复项）**

如有修复，按任务各自提交；无修复则本任务无提交（验证通过即完成）。

---

## Self-Review 结论

- **Spec 覆盖**：设计文档 §5 字段（10 列）→ Task 1；§6.1 客户端 → Task 3；§6.2 同步服务 → Task 4+6；§6.3 控制器 → Task 7；§7 人工锁定 → Task 2+4；§8 前端 → Task 8；§9 测试 → 各任务 TDD 步骤；§10 风险（超时/冲突/旧模型）→ Task 3/6 测试与实现；§12 里程碑 M1-M5 → Task 1-9。
- **占位符扫描**：无 TBD/TODO；Task 7/8 中"对齐现有文件风格"类指引均指向可读取的具体文件。
- **类型一致性**：`ModelCatalogDto` 字段名（id/name/description/family/attachment/reasoning/toolCall/structuredOutput/knowledge/releaseDate/lastUpdated/openWeights/modalities/limit/license/benchmarks/weights）在 Task 3/4/6 的测试与实现中一致；`CatalogSyncReport` getter（isSuccess/getAddedCount/...）在 Task 6/7 一致；`Model.lockedFields` 在 Task 1/2/4/6 一致；`findByExternalId` 签名在 Task 1/6 一致。
- **已知待执行者确认点**：Task 7/8 中 `@WebMvcTest`/前端 http 封装的具体写法需读取对应现有文件后对齐（已注明文件路径）。
